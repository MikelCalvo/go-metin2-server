# Packet matrix

This is the working packet inventory for the first protocol milestone.
It is intentionally narrow and only covers the boot path plus the first in-world movement step.

Status values:
- `documented` — the packet is part of the current written contract
- `planned` — known to be needed soon, but not yet locked by tests
- `candidate` — observed or expected, but still awaiting final confirmation in our own suite

Planned rows may temporarily use `Header = TBD` when the project freezes the family name and scope before the exact wire header/layout reaches packet tests.

## Control plane

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `PONG` | client -> server | `0x0006` | handshake/game control | documented | accepted as a header-only reply to server `PING`; phase-stable in `HANDSHAKE` and `GAME` |
| `PING` | server -> client | `0x0007` | handshake/game control | documented | carries `server_time`; current slice freezes the codec and the matching `PONG` reply path |
| `PHASE` | server -> client | `0x0008` | control | documented | phase transition control packet; on the main game socket the observed client bootstrap expects `PHASE(HANDSHAKE)` before the first `KEY_CHALLENGE` |
| `KEY_RESPONSE` | client -> server | `0x000A` | handshake | documented | cryptographic response path |
| `KEY_CHALLENGE` | server -> client | `0x000B` | handshake | documented | challenge + server key material; authd may send it immediately after connect, while the main game socket should follow the observed `PHASE(HANDSHAKE)` prelude |
| `KEY_COMPLETE` | server -> client | `0x000C` | handshake | documented | completes key exchange |
| `CLIENT_VERSION` | client -> server | `0x000D` | loading | documented | accepted as metadata in `LOADING`; no server response and no phase transition |
| `STATE_CHECKER` | client -> server | `0x000F` | control probe | documented | separate channel-status probe socket; header-only request outside the normal login/game choreography |
| `RESPOND_CHANNELSTATUS` | server -> client | `0x0010` | control probe | documented | channel-status response consumed by the dedicated state-checker client path |

## Authentication and selection surface

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `LOGIN2` | client -> server | `0x0101` | login | documented | minimal login-by-key request frozen by tests |
| `LOGIN3` | client -> server | `0x0102` | auth | documented | minimal auth-server credential request frozen by tests |
| `LOGIN_SECURE` | client -> server | `0x0103` | login | candidate | not part of the first implementation unless required |
| `LOGIN_SUCCESS3` | server -> client | `0x0104` | login/select | candidate | possible older character-list success shape |
| `LOGIN_SUCCESS4` | server -> client | `0x0105` | login/select | documented | minimal selection-surface success path frozen by tests |
| `LOGIN_FAILURE` | server -> client | `0x0106` | auth/login | documented | negative auth/login path frozen by tests |
| `LOGIN_KEY` | server -> client | `0x0107` | login | candidate | not part of the current minimal login-by-key happy path |
| `AUTH_SUCCESS` | server -> client | `0x0108` | auth | documented | minimal authd success path with issued login key frozen by tests |
| `EMPIRE` | server -> client | `0x0109` | login/select | documented | selection surface empire state frozen by tests |
| `EMPIRE` | client -> server | `0x010A` | select | documented | minimal empire selection request for empty-account bootstrap flow |

## Character lifecycle

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `CHARACTER_CREATE` | client -> server | `0x0201` | select | documented | create character request |
| `CHARACTER_DELETE` | client -> server | `0x0202` | select | documented | delete character request with private-code field |
| `CHARACTER_SELECT` | client -> server | `0x0203` | select | documented | choose active character |
| `ENTERGAME` | client -> server | `0x0204` | loading | documented | accepted request; current exact `LOADING -> GAME` burst is frozen by `loading-to-game-bootstrap-burst.md` |
| `CHARACTER_ADD` | server -> client | `0x0205` | game bootstrap/visibility | documented | used for the selected-character bootstrap, for peer visibility, and now also for the first bootstrap static-actor enter-game inserts |
| `CHAR_ADDITIONAL_INFO` | server -> client | `0x0207` | game bootstrap/visibility | documented | metadata companion for visible player inserts and the current bootstrap static-actor enter-game burst; player payloads now also carry the first bootstrap equipment-appearance projection for equipped `body` / `weapon` / `head` / `hair` items, using template-authored `appearance_vnum` when present and falling back to the equipped item `vnum` otherwise |
| `CHARACTER_DEL` | server -> client | `0x0208` | game visibility | documented | removes a visible peer by `vid` when that actor leaves the local world |
| `CHARACTER_UPDATE` | server -> client | `0x0209` | game bootstrap/visibility | documented | first self-only state refresh after the visible-world insert, the current self-only equip/unequip appearance refresh payload, the current queued peer-visible equip/unequip appearance refresh payload for already-visible stable peers, the current peer-visibility refresh payload, and the current bootstrap static-actor enter-game refresh payload; player payloads now also reuse the first bootstrap equipment-appearance projection for equipped `body` / `weapon` / `head` / `hair` items, using template-authored `appearance_vnum` when present and falling back to the equipped item `vnum` otherwise |
| `CHARACTER_POSITION` | server -> client | `0x020B` | game | documented | first server stance/position presentation codec; payload is little-endian `uint32 vid` plus raw `uint8 position`; the shipped runtime now emits it self-only and to currently visible live peers for accepted client `CHARACTER_POSITION(position=0|4)` presentation requests, while battle/unsupported position bytes still fail closed and combat/death/restart/respawn paths do not add extra stance packets |
| `PLAYER_CREATE_SUCCESS` | server -> client | `0x020C` | select | documented | create success result |
| `PLAYER_CREATE_FAILURE` | server -> client | `0x020D` | select | documented | create failure result |
| `PLAYER_DELETE_SUCCESS` | server -> client | `0x020E` | select | documented | delete success result carrying the cleared account slot |
| `PLAYER_DELETE_FAILURE` | server -> client | `0x020F` | select | documented | minimal delete rejection placeholder using a header-only failure frame |
| `MAIN_CHARACTER` | server -> client | `0x0210` | loading | documented | main actor bootstrap |
| `PLAYER_POINTS` | server -> client | `0x0214` | loading/game bootstrap | documented | initial stat payload |
| `PLAYER_POINT_CHANGE` | server -> client | `0x0215` | loading/game bootstrap / game | documented | first self-only point refresh after the selected-character bootstrap; below-floor selected-character HP snapshots clamp this `ENTERGAME` refresh to `amount = 0`, `value = 0` before the self `DEAD(owner_vid)` replay; the first owned consumable-use vertical also reuses it for the selected character using template-authored signed `use_effect` metadata (`type`, signed `amount`, point index, and optional stack `consume_count`), while the current seeded bootstrap consumable still resolves to `type = 1`, `amount = 50`, default `consume_count = 1`, and `value = updated Points[1]`; negative consumable deltas are now owned as self-only point decreases and fail closed on signed 32-bit underflow before item/point/quickslot/persistence mutation; authored multi-count consumables decrement exactly that many stack units and fail closed when the live stack has too few units; the first narrow template-backed equip/unequip point slice also reuses it self-only when the matched equipped item carries `equip_effect`, with the current seeded practice blade resolving to `vnum = 12200`, `type = 1`, and `amount = +/-10` on equip/unequip and the current seeded cursed practice blade resolving to `vnum = 12201`, `type = 1`, and `amount = -10` on equip / `+10` on unequip; engaged content-loaded `spawn_groups` practice mobs now also reuse it for both the immediate self-only owner retaliation piggyback on accepted live hits and one delayed self-only server-origin follow-up beat after the first accepted live owner hit, with later autonomous beats continuing every owned `1s` only while the same engagement remains live and failing closed if target intent clears/replaces or the mob dies / rebuilds before the pending beat fires |
| `STUN` | server -> client | `0x0216` | game | documented | first owned stun-signal codec; payload is little-endian `uint32 vid`; the Go runtime does not emit this packet yet, and current death/combat paths continue to use `DEAD`, `TARGET`, `PLAYER_POINT_CHANGE`, and `DAMAGE_INFO` without adding a stun companion until a later stun/knockdown policy slice is frozen |
| `DEAD` | server -> client | `0x0217` | game | documented | first owned visible death signal; payload is little-endian `uint32 vid`; the current bootstrap death/respawn contract uses it when a visible `training_dummy` reaches runtime HP `0`, while the current practice-mob retaliation slice also reuses it for an engaged owner's live HP floor at `0` with self `DEAD + TARGET(0, 0)` plus one visibility-gated peer `DEAD(owner_vid)` fanout; fresh `ENTERGAME` bootstrap for an already-`0`-HP or below-floor selected-character snapshot appends self `DEAD(owner_vid)` after the selected-character point/quickslot replay and before trailing world visibility frames, with below-floor point refreshes clamped to the visible `0`-HP floor; broader player death / respawn choreography remains out of scope |
| `CHANGE_SPEED` | server -> client | `0x0218` | game | documented | first server movement-speed presentation codec; payload is little-endian `uint32 vid` plus little-endian `uint16 moving_speed`; currently codec/documentation only and not emitted by bootstrap movement, combat, skill, item/equipment, death, restart, or respawn runtime paths |

## Movement

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `MOVE` | client -> server | `0x0301` | game | documented | first in-world action; an exact-position bootstrap transfer trigger may consume the packet without the normal same-map reply path, and the current transfer rebootstrap result also rebuilds static-actor and pending ground-item visibility deltas for the moved socket |
| `MOVE` | server -> client | `0x0302` | game | documented | deterministic self ack for the normal same-map mover path and current queued replication payload for already-visible peers on the same bootstrap `MapIndex`; the current transfer-trigger path intentionally suppresses the immediate self `MOVE_ACK` and returns the transfer rebootstrap burst documented in `transfer-rebootstrap-burst.md` instead |
| `SYNC_POSITION` | client -> server | `0x0303` | game | documented | first selected-character position reconciliation path in `GAME`; an exact-position bootstrap transfer trigger may consume the packet without the normal same-map reply path, and the current transfer rebootstrap result also rebuilds static-actor and pending ground-item visibility deltas for the moved socket |
| `SYNC_POSITION` | server -> client | `0x0304` | game | documented | deterministic selected-character sync reply for the normal same-map sender path and current queued replication payload for already-visible peers on the same bootstrap `MapIndex`; the current transfer-trigger path intentionally suppresses the immediate self `SYNC_POSITION_ACK` and returns the transfer rebootstrap burst documented in `transfer-rebootstrap-burst.md` instead |
| `WARP` | client -> server | `0x0305` | game | planned | out of early scope; the bootstrap runtime currently reuses `MOVE` / `SYNC_POSITION` exact-position triggers instead of owning a dedicated warp request |
| `WARP` | server -> client | `0x0306` | game | planned | out of early scope; the bootstrap runtime does not yet freeze a final self-visible warp/loading packet and currently uses the self-session transfer contract documented in `transfer-rebootstrap-burst.md` |

## Chat

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `CHAT` | client -> server | `0x0601` | game | documented | current slice accepts `CHAT_TYPE_TALKING`, bootstrap `CHAT_TYPE_PARTY`, bootstrap `CHAT_TYPE_GUILD`, bootstrap `CHAT_TYPE_SHOUT`, and bootstrap `CHAT_TYPE_INFO`; once the later player-death retaliation floor has already reached `0` HP for the selected owner, peer-facing `CHAT` types plus self-only `CHAT_TYPE_INFO` fail closed instead; client-originated `CHAT_TYPE_NOTICE` is currently rejected; bootstrap `CHAT_TYPE_TALKING` currently also hosts the server-owned slash seams `/restart_here`, `/restart_town`, `/inventory_move`, `/equip_item`, `/unequip_item`, `/use_item <slot>`, and the temporary local-only merchant debug harness `/shop_buy <catalog_slot>` until richer dedicated gameplay families replace those temporary harness paths |
| `WHISPER` | client -> server | `0x0602` | game | documented | current slice routes by exact target character name with variable text payload |
| `CHAT` | server -> client | `0x0603` | game | documented | deterministic sender echo for talking/party/guild/shout; current fanout scope is same-map + same-empire for talking, same empire for shout, same non-zero `GuildID` for guild, and all connected sessions for party; sender-only bootstrap system info uses `vid = 0`; template-backed item/equipment guard feedback such as authored irremovable-unequip rejection text also uses self-only `CHAT_TYPE_INFO` with `vid = 0`; server-originated notice broadcasts also use `vid = 0` raw system text |
| `WHISPER` | server -> client | `0x0604` | game | documented | direct whisper delivery to the named target on success and current `WHISPER_TYPE_NOT_EXIST` sender feedback for unknown targets |

## Interaction

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `INTERACT` | client -> server | `0x0501` | game | documented | first owned client-originated interaction request for a visible bootstrap static actor target by `vid`; current payload is a little-endian `uint32 target_vid`; because this bootstrap header overlaps `ITEM_USE`, `internal/game` routes `0x0501` by payload length (`3` bytes for `ITEM_USE`, `4` bytes for `INTERACT`) until a later client-facing interaction packet contract replaces the temporary overlap; current owned responses include self-only `info` / `talk` / `shop_preview`, the current service-style NPC families also include transfer-backed `warp`, and dead visible interactables fail closed with a self-only `target_dead` info-chat response before resolving authored content |

## Merchants / shops

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `SHOP` | client -> server | `0x0801` | game | documented | first owned merchant-window client family freezes subheaders `BUY` and `END` on top of a merchant session opened from the existing `INTERACT` ingress; `BUY` remains valid only while an active merchant context exists, the second trailing byte after the common `SHOP` envelope is the zero-based authored `catalog_slot`, `END` now also fails closed after the current practice-mob retaliation floor has already cleared that merchant context on owner death, and `SELL(slot)` / `SELL2(slot,count)` now route to the active merchant sell-back runtime path for carried-slot stack removal/decrement with invalid/malformed/equipped/over-template-max/sell-credit-overflow/anti-sell/job-sex-restricted/stale cases failing closed |
| `SHOP` | server -> client | `0x0810` | game | documented | first owned merchant-window server family freezes `START` as the open response, now with resolved item-template display `sockets` and `attributes` copied into each non-empty catalog entry, and `END` as the explicit close companion while the socket can still deliver merchant frames; the same `END` family now also tears down an already-open merchant window after the current practice-mob retaliation floor reaches the owner at `0` HP, prepends the self transfer rebootstrap burst when a successful warp or exact-position transfer closes that same still-live merchant window, prepends the select-phase transition when a same-socket `/phase_select` closes that merchant window, auto-closes stale packet-buy windows whose live actor/context or authored catalog changed underneath them, and prepends template-backed safebox rejection feedback or template-backed refine rejection/preview feedback when `SAFEBOX_CHECKIN` or `REFINE` closes an active merchant window; packet `SHOP BUY` now owns self-only `ITEM_SET` refreshes as the complete success companion without an extra bare `OK`, plus bare no-payload `NOT_ENOUGH_MONEY`, `INVENTORY_FULL`, and `INVALID_POS` error companions for insufficient-gold, no-valid-placement, unknown-slot, anti-get/template-acquisition, and selected-character restriction failures, with anti-get failures appending a template-authored/fallback self-only `CHAT_TYPE_INFO` rejection after `INVALID_POS` and selected-character failures appending authored `buy_reject_message` only when present; packet `SHOP SELL` / `SELL2` success now emits carried-slot `ITEM_DEL` for whole-stack removal or `ITEM_UPDATE` for partial-stack count refresh, then self-only `PLAYER_POINT_CHANGE(POINT_GOLD)` without appending an extra bare `OK`; successful packet `SHOP BUY` / `SELL` / `SELL2` also closes an active same-socket exchange shell first with self/peer `GC::EXCHANGE END` before the merchant transaction refresh frames; sell credit prefers non-zero template-authored `shop_sell_price` before derived `shop_buy_price` / `sell_count_per_gold` fallback; invalid slots, malformed carried items, explicit zero-count `SELL2`, live carried stacks above the resolved template-authored `max_count`, template sell credit/resulting gold values that exceed the current point-change carrier, selected-character job/sex restriction and runtime-locked rejections reuse `INVALID_POS`, while `anti_sell` appends a template-authored/fallback self-only `CHAT_TYPE_INFO` rejection after `INVALID_POS` and selected-character sell failures append authored `sell_reject_message` only when present; the reusable `UPDATE_ITEM` shop-slot refresh, `UPDATE_PRICE` signed-Elk refresh, bare no-payload `SOLDOUT`, `SOLD_OUT`, and `NOT_ENOUGH_MONEY_EX` result codecs, and `START_EX` multi-tab extended-shop open codec are now documented for later stock/player-shop/extended-shop slices |
| `MYSHOP` | client -> server | `0x0802` | game | documented | codec-owned private-shop open request in `internal/proto/shop`: fixed `sign[33]` + `count uint8` plus `count` packed 13-byte `TShopItemTable` entries (`vnum`, `count`, packed `TItemPos`, `price`, `display_pos`); decode rejects unexpected header, `count > 40`, and length/count mismatch; next frozen GAME seam is deny-no-response dispatch (`DecodeClientMyShop` + default `Accepted:false`, no frames / no disconnect) before any accepted private-shop mutation or `GC::SHOP_SIGN` companion |

## Items, inventory, and equipment

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `ITEM_USE` | client -> server | `0x0501` | game | documented | first owned client-originated item-use request; payload is packed `TItemPos(window:uint8, cell:uint16)`; current live runtime accepts only carried inventory positions (`window = INVENTORY`, `cell < 90`), fails closed for locked, malformed, `quest_use`, `quest_use_multiple`, `applicable`, selected-character job/sex-restricted carried-slot items, or signed point overflow/underflow; templates with `confirm_when_use` follow the ordinary consumable success path after the client-local confirm dialog, and routes accepted requests through the same bootstrap template-backed consumable path already exercised by `/use_item <slot>`; accepted packet ingress now prepends the self-only server `ITEM_USE` echo before point/item/info refreshes; the path accepts both positive and negative signed template `use_effect.point_delta` values, consumes template-authored `use_effect.consume_count`, uses non-empty `use_effect.info_message` for successful-use placeholder `CHAT_TYPE_INFO` text while falling back to `use_effect.message` when omitted, and uses non-empty template-authored `use_reject_message` as one self-only `CHAT_TYPE_INFO` for guarded no-mutation direct-use rejections before any hypothetical point delta from the rejected item is applied or range-checked, closing an active same-socket merchant window with self `GC::SHOP END` and/or active exchange shell with self/peer `GC::EXCHANGE END` before that rejection chat |
| `ITEM_USE` | server -> client | `0x0512` | game | documented | accepted client-originated carried consumable echo; payload is `TItemPos cell`, `character_vid uint32`, `victim_vid uint32`, and `vnum uint32`; the bootstrap self-only runtime sets both VID fields to the selected character `VID` and emits this echo only for packet ingress, not for the slash `/use_item` harness |
| `ITEM_USE_TO_ITEM` | client -> server | `0x0506` | game | documented | first owned drag-to-item item-use path; payload is source packed `TItemPos` followed by target packed `TItemPos`; current live runtime accepts only carried inventory stack-on-stack consolidation for compatible same-`vnum` stackable templates whose authored `max_count` fits the owned one-byte item refresh count range (`<= 255`), emits the same self-only inventory refresh family as carried `ITEM_MOVE`, persists the merged inventory, uses `ITEM_UPDATE` for the already-known target stack on full and partial merges so template-authored sockets/attributes stay attached to count-only refreshes, deletes only source item quickslots when a full merge removes that source stack, closes an active same-socket exchange shell before accepted merge refresh frames, and fails closed without normal `use_effect` fallback for incompatible or unsupported targets; empty source/target slots, same-cell source/target requests, non-stackable templates, authored `anti_stack`/`anti_drop`/`anti_give`/`anti_sell` templates, selected-character job/sex anti-flag templates, locked source/target stacks, and over-template-max stacks are verified as no-frame/no-mutation guards |
| `ITEM_MOVE` | client -> server | `0x0504` | game | documented | first owned client-originated carried-slot move/split/merge request; payload is source packed `TItemPos`, destination packed `TItemPos`, and `count uint8`; current live runtime accepts only carried inventory positions (`window = INVENTORY`, `cell < 90`), treats `count = 0` as a full-stack move that succeeds into an empty destination, swaps with an incompatible occupied destination, or merges into a compatible occupied destination up to the source template `max_count`, accepts empty-destination partial splits and template-bounded compatible occupied-destination merges for non-zero counts, treats a non-zero count that covers the whole source stack as the same full-stack carried move/swap path for incompatible occupied destinations, fails closed for partial incompatible occupied-destination counted moves, oversized non-zero counts, template-`max_count` overflow, storage-overflowing destination stack counts, locked source/destination slots, occupied source/target cells whose live counts already exceed the corresponding authored `max_count`, template-backed equipment equip/unequip source counts above authored `max_count`, authored `irremovable` equipment unequip (which returns one self-only `CHAT_TYPE_INFO` rejection instead of item mutation frames), and authored item-template snapshots that combine `stackable` with `equip_slot` until richer stack and item-lock semantics are owned, reuses the selected-character inventory persistence boundary, keeps identity-changing moves/splits/swaps on self-only `ITEM_DEL` / `ITEM_SET`, requires both source and target item templates for incompatible occupied-destination swaps when an authored item-template store is loaded so both resulting `ITEM_SET` refreshes carry the resolved authored metadata, uses self-only `ITEM_UPDATE` count refreshes for compatible occupied-destination partial merges, emits `ITEM_DEL(source)` then `ITEM_UPDATE(destination)` for exact counted or zero-count compatible full-stack merges, preserves loaded template display sockets/attributes on all compatible merge `ITEM_UPDATE` frames, synchronizes item quickslots only for moves where the source item identity leaves its carried cell, accepts authored equip/unequip metadata through equipment cells, projects authored equipment appearance through `CHARACTER_UPDATE`, closes an active same-socket exchange shell before successful carried/equipment mutation refreshes, accepts occupied-wear swap/replace onto an already-occupied matching wear cell when the source cell can accept the worn item (`ITEM_SET` source with former worn item, `ITEM_SET` equipment with former source item, invert-then-apply `PLAYER_POINT_CHANGE`, `CHARACTER_UPDATE`), keeps non-swappable occupied destinations on self-only `CHAT_TYPE_INFO` `You are already wearing equipment.`, and keeps `irremovable` previous worn templates on the owned unequip reject chat, and now also closes that active exchange shell before template-authored equipment rejection feedback for guarded equip, occupied-wear equip reject, or `irremovable` unequip |
| `ITEM_DROP` | client -> server | `0x0502` | game | documented | drop request with packed `TItemPos` plus `elk uint32`; the shipped runtime now accepts carried-slot drops as self-only inventory removal plus `ITEM_GROUND_ADD` + `ITEM_OWNERSHIP`, while loaded template transfer guards, selected-character restrictions, and deterministic ground-`VID` collisions fail closed before mutation; stale/reclaimed sockets that no longer own the live shared-world session may receive only self-local inventory/quickslot removal frames and never register or emit a bootstrap ground handle; successful carried-item drops close an active same-socket exchange shell first, emitting self `GC::EXCHANGE END` before drop frames and peer `END` before queued visible ground frames; template-backed rejection feedback also closes an active same-socket exchange shell first with self/peer `GC::EXCHANGE END`; transfer-guard rejection info chat uses template-authored `drop_reject_message` when present and falls back to the deterministic default when omitted, while selected-character restriction rejections emit that authored text only when present |
| `ITEM_DROP2` | client -> server | `0x0503` | game | documented | counted drop request with packed `TItemPos` plus `gold uint32` and `count uint8`; the shipped runtime now accepts non-zero carried-slot counts that fit the source stack as self-only inventory decrement/removal plus `ITEM_GROUND_ADD` + `ITEM_OWNERSHIP`, while loaded template transfer guards, selected-character restrictions, and deterministic ground-`VID` collisions fail closed before mutation; stale/reclaimed sockets that no longer own the live shared-world session may receive only self-local inventory/quickslot refresh frames and never register or emit a bootstrap ground handle; successful carried-item drops close an active same-socket exchange shell first, emitting self `GC::EXCHANGE END` before drop frames and peer `END` before queued visible ground frames; template-backed rejection feedback also closes an active same-socket exchange shell first with self/peer `GC::EXCHANGE END`; transfer-guard rejection info chat uses template-authored `drop_reject_message` when present and falls back to the deterministic default when omitted, while selected-character restriction rejections emit that authored text only when present |
| `ITEM_PICKUP` | client -> server | `0x0505` | game | documented | pickup request with `vid uint32`; `internal/game` dispatch is frozen, and the shipped runtime now accepts visible-world pickup of temporary ground handles created by accepted carried-slot drops; stackable owner pickup first tries a full merge into the lowest compatible carried stack before falling back to empty-slot placement, valid authored equipment templates are picked up as carried inventory items instead of being auto-equipped, merge `ITEM_UPDATE` refreshes preserve template-authored sockets/attributes when metadata is available, template-backed transfer/selected-character restriction rejections use authored `pickup_reject_message` when present while falling back to the deterministic inventory-full info text, authored `pickup_range` now gates bootstrap gold markers when present on `vnum = 1`, peer pickup of authored `vnum = 1` gold markers now honors `anti_give` with the same pickup rejection text before owner/collector currency mutation, and accepted pickup or pickup feedback closes an active same-socket exchange shell first with self/peer `GC::EXCHANGE END` before pickup frames or info chat |
| `ITEM_GIVE` | client -> server | `0x0507` | game | documented | first owned item-give ingress; payload is `target_vid uint32`, packed `TItemPos(window:uint8, cell:uint16)`, and `count uint8`; `internal/game` decodes and dispatches it in `GAME`, while the shipped minimal runtime currently rejects ordinary attempts fail-closed with no frames, inventory, quickslot, ground-handle, peer, or persistence mutation until a later transfer/exchange slice owns recipient semantics; the one template-authored exception is carried inventory items whose valid template authors `anti_give` plus non-empty `give_reject_message`, whose `target_vid` names a currently connected visible player, and whose requested count is non-zero and not larger than the live carried stack, which return one self-only `CHAT_TYPE_INFO` guard message with no target frames or item-state mutation, closing an active same-socket merchant window first with self-only `GC::SHOP END` and/or active exchange shell with self/peer `GC::EXCHANGE END`; zero/unknown/invisible targets stay silent fail-closed |
| `EXCHANGE` | client -> server | `0x0508` | game | documented | first owned item-exchange ingress; payload is `subheader uint8`, `arg1 uint32`, `arg2 uint8`, and packed `TItemPos(window:uint8, cell:uint16)`; current owned client subheaders are `START`, `ITEM_ADD`, `ITEM_DEL`, `ELK_ADD`, `ACCEPT`, and `CANCEL`; `internal/game` decodes and dispatches it in `GAME`; the shipped minimal runtime now accepts a visible connected peer `START` shell only when that peer also satisfies the owned exchange-distance gate (`ApproxDistance < 1000`), the requester does not already have an open same-socket merchant window (requester-busy returns self-only `CHAT_TYPE_INFO` `You cannot trade while another trade window is open.` with the merchant window left open), and the peer does not currently have an open same-socket bootstrap merchant window (partner-busy returns self-only `CHAT_TYPE_INFO` `That player cannot trade right now.` with no pairing), gold-carrier-cap `START` / `ACCEPT` / commit-time reject chat when either side already holds or drifts to `>= 1<<31-1` gold (requester `You have more than 2 Billion Yang. You cannot trade.` / partner `The player has more than 2 Billion Yang. You cannot trade with him.`, local-first requester-wins), display-only carried-item `ITEM_ADD` while that shell is active with duplicate display-slot/source-item attempts suppressed, display-only `ITEM_DEL` for an occupied requester display slot, display-only `ELK_ADD` when the requested amount is non-zero and not above live gold, active-shell display-only `ACCEPT`, paired `CANCEL` close path, and self-only busy-target `ALREADY` / too-much-gold `LESS_GOLD` responses, while ordinary unsupported requests remain fail-closed with no inventory/equipment, quickslot, gold, ground-handle, or persistence mutation; when the second accept passes those checks the runtime owns the first mutual-accept finalize (item/gold transfer, source quickslot clear, dual account persistence, dual-sided success info-chat `The trade with <partner_name> has been successful.`, shell `END`), and origin/partner persistence failures fail closed with no trade mutation while leaving the shell cancellable; display-only `ITEM_ADD` emits self `GC::EXCHANGE ITEM_ADD(is_me=1)` and queued peer `GC::EXCHANGE ITEM_ADD(is_me=0)` using template-authored sockets/attributes without removing or persisting the carried item, while same-socket mutations of that still-displayed carried cell (`ITEM_USE`, carried move/equip/unequip, `ITEM_USE_TO_ITEM`, drop, merchant sell, `SAFEBOX_CHECKIN` of that displayed source cell, and `SAFEBOX_CHECKOUT` into that displayed destination cell) fail closed with no frames and leave the shell open; display-only `ITEM_DEL` emits self/peer clear frames and frees the in-memory display slot/source-item identity; display-only `ELK_ADD` emits self/peer gold frames without changing live or persisted gold and records the displayed amount for later accept-time revalidation; display-only `ACCEPT` emits self/peer accept marker frames with `arg1 = 1` without closing the shell or finalizing trade only when the requester's remembered displayed carried items still exactly match live item id/`vnum`/count/slot state, the currently loaded item-template metadata still allows each displayed carried item, any requester remembered displayed gold is still within live gold, any already-accepted partner display state still matches that partner's current shared-world character state and current item-template metadata, and second-accept receiver item-id-collision, over-template-max compatible-stack, inventory-capacity, gold-overflow, and either-side open merchant/safebox/refine busy-window preconditions pass; stale requester displayed items fail closed silently, stale requester displayed gold emits one self-only `LESS_GOLD` and no peer accept marker, stale already-accepted partner displayed items or gold fail closed silently, and either-side open merchant/safebox/refine busy-window accept rejects emit the owned busy info-chat, receiver inventory-capacity failures emit dual-sided Space info-chat, receiver gold-overflow failures emit dual-sided gold-overflow info-chat, and receiver item-id collision / over-template-max compatible-stack / locked-compatible-stack / selected-character restriction failures fail closed silently; later accepted `ITEM_ADD`, `ITEM_DEL`, or in-budget `ELK_ADD` display changes clear prior accept markers with `GC::EXCHANGE ACCEPT(arg1 = 0)` before the display frame; active-shell carried inventory `ITEM_ADD` attempts whose valid template authors one owned exchange-display rejection guard (`anti_stack`, `anti_get`, `anti_drop`, `anti_give`, `anti_sell`, job/sex/empire anti flags, or `min_level`) plus non-empty `give_reject_message` keep the self-only `CHAT_TYPE_INFO` guard message instead of exchange display; same-socket `/quit`, `/logout`, `/phase_select`, successful carried-item use, template-authored direct item-use rejection feedback, successful carried/equipment item movement, successful `ITEM_USE_TO_ITEM` stack consolidation, successful carried-item drop, template-authored carried-drop rejection feedback, accepted pickup, template-backed pickup rejection feedback, successful merchant `SHOP BUY` / `SELL` / `SELL2`, template-authored safebox-checkin rejection, template-authored `REFINE` rejection/preview feedback, and template-authored `ITEM_GIVE` anti-give feedback close an open exchange shell before returning lifecycle/item/equipment/pickup/merchant/storage/refine/item-give frames |
| `EXCHANGE` | server -> client | `0x051C` | game | documented | first shared server exchange response codec; payload is `subheader uint8`, `is_me uint8`, `arg1 uint32`, packed `TItemPos`, `arg3 uint32`, three sockets, and seven attributes; current owned server subheaders are `START`, `ITEM_ADD`, `ITEM_DEL`, `GOLD_ADD`, `ACCEPT`, `END`, `ALREADY`, and `LESS_GOLD`; the shipped runtime emits the current paired `START` / `END` window shell for visible connected peers inside the owned exchange-distance gate, plus self-only gold-carrier-cap `START` / `ACCEPT` / commit-time reject chat when either side already holds or drifts to `>= 1<<31-1` gold, plus walk-away `END` when either paired player relocates outside that gate, plus transfer/warp-rebootstrap `END` that prepends self `END` before the transfer burst and queues peer `END` even for same-map in-range destinations, display-only `ITEM_ADD` frames for active-shell carried-item placement, display-only `ITEM_DEL` frames carrying the cleared display slot in `arg1`, display-only `GOLD_ADD` frames carrying the displayed amount in `arg1`, display-only `ACCEPT` marker frames carrying `arg1 = 1`, accept-reset frames carrying `arg1 = 0` before accepted display changes, self-only `LESS_GOLD` for active-shell over-live-gold attempts and requester-side accept-time stale displayed-gold revalidation, silent no-frame rejection for requester-side stale displayed items/template metadata and second-accept receiver item-id-collision/over-template-max compatible-stack/locked-compatible-stack/selected-character restriction failures, dual-sided Check info-chat for already-accepted partner-side stale displayed items/template metadata/gold, dual-sided Space info-chat for second-accept inventory-capacity failures, dual-sided gold-overflow info-chat for receiver gold-overflow failures, self-only busy info-chat for either-side open merchant/safebox/refine busy-window accept rejects, dual-sided success info-chat `The trade with <partner_name> has been successful.` on mutual-accept finalize before shell `END`, a self-only `ALREADY` response for visible busy targets, and self/peer `END` frames when same-socket `/quit`, `/logout`, `/phase_select`, successful non-displayed carried-item use, template-authored direct item-use rejection feedback, successful non-displayed carried/equipment item movement, successful non-displayed `ITEM_USE_TO_ITEM` stack consolidation, successful non-displayed carried-item drop, template-authored carried-drop rejection feedback, accepted pickup, template-backed pickup rejection feedback, successful merchant `SHOP BUY` / non-displayed `SELL` / `SELL2`, template-authored equipment equip/unequip rejection feedback, template-authored safebox-checkin rejection, template-authored `REFINE` rejection/preview feedback, or template-authored `ITEM_GIVE` anti-give feedback tears down an open exchange shell before lifecycle/item/equipment/pickup/merchant/storage/refine/item-give frame completion, while richer finalize/result choreography beyond the first mutual-accept mutation + shell close, and stronger rollback/audit policy beyond fail-closed origin/partner persistence rollback, remain future exchange-state work |
| `REFINE` | client -> server | `0x050C` | game | documented | first owned refine ingress; payload is `pos uint8` plus `type uint8`; `internal/game` decodes and dispatches it in `GAME`; current shipped behavior keeps ordinary result semantics fail-closed except for authored non-refineable `refine_reject_message` info-chat rejection, authored refineable `refine_info` self-only `REFINE_INFORMATION_NEW` preview (either of which first closes active same-socket merchant/exchange presentation shells before the refine feedback frame), `type = 255` cancel of an open same-socket refine dialog with no frames/mutation, and the accepted confirm-after-preview paths for `probability = 100` (matching confirm consumes gold/materials, replaces source `vnum` with `result_vnum` in-place, persists, and emits self-only material refresh + material-removal `GC::QUICKSLOT_DEL` + result `ITEM_SET` + gold `PLAYER_POINT_CHANGE` + `CHAT_TYPE_COMMAND` `RefineSuceeded <type>`), `probability = 0` (matching confirm consumes gold/materials, destroys the source cell, persists, and emits self-only material refresh + material/source `GC::QUICKSLOT_DEL` + source `ITEM_DEL` + gold `PLAYER_POINT_CHANGE` + `CHAT_TYPE_COMMAND` `RefineFailed <type>`), and `probability` in `1..99` (matching confirm draws one injected roll via `takeRefineConfirmRoll()`; `roll <= probability` → owned success burst / `RefineSuceeded`; `roll > probability` → owned destroy burst / `RefineFailed`, including destroy-source quickslot sync); catalysts, keep-grade/downgrade variants, and busy merchant/exchange/safebox confirm attempts remain deferred/fail-closed |
| `REFINE_INFORMATION` | server -> client | `0x051D` | game | documented | owned refine-dialog information frame layout; payload is `type uint8`, `pos uint8`, and a fixed refine table (`src_vnum`, `result_vnum`, `material_count`, `cost`, `prob`, five `{vnum,count}` material rows); decode/encode reject `material_count > 5`; the shipped runtime currently uses the newer `REFINE_INFORMATION_NEW` header for template-authored previews rather than this older header |
| `REFINE_INFORMATION_NEW` | server -> client | `0x051E` | game | documented | same fixed refine-dialog information payload as `REFINE_INFORMATION`, under the newer client-facing header whose UI path also forwards the `type` byte; the shipped runtime now emits one self-only frame only when a valid carried item resolves to a refineable template with authored `refine_info`, selected-character restrictions satisfied, and transfer guards unset; that preview opens the same-socket dialog presentation used by the shipped confirm-after-preview success path (`probability = 100`), destroy-failure path (`probability = 0`), and injected-roll confirm path (`probability` in `1..99` → owned success or destroy bursts including destroy-source quickslot sync), while catalyst and keep-grade/downgrade variants remain deferred |
| `SAFEBOX_CHECKIN` | client -> server | `0x0820` | game | documented | first owned safebox check-in ingress; payload is `safe_pos uint8` plus packed `TItemPos`; `internal/game` decodes it in `GAME` through an explicit fail-closed-by-default handler seam; while `/open_safebox` is already open the shipped minimal runtime accepts one unlocked unequipped carried stack into an empty in-range safebox cell (not `anti_safebox`), removes that carried stack, syncs source item quickslots with self-only `GC::QUICKSLOT_DEL` when the cell is fully cleared, stores same-session in-memory safebox contents, persists the inventory/quickslot account snapshot, and emits self-only `ITEM_DEL` + `SAFEBOX_SET` (closing an active merchant window and/or exchange shell first when present); closed presentation, occupied/out-of-range destinations, exchange-displayed source cells, and other malformed attempts stay fail-closed with no frames; the authored feedback exception remains a valid carried item whose template authors both `anti_safebox` and `safebox_reject_message`, which returns one self-only `CHAT_TYPE_INFO` with that authored text and still performs no mutation, closing an active same-socket merchant window first with self-only `GC::SHOP END` and/or active exchange shell with self/peer `GC::EXCHANGE END` when present |
| `SAFEBOX_CHECKOUT` | client -> server | `0x0821` | game | documented | first owned safebox checkout ingress; payload is `safe_pos uint8` plus packed `TItemPos`; `internal/game` decodes it in `GAME` through an explicit fail-closed-by-default handler seam; while `/open_safebox` is already open the shipped minimal runtime accepts one remembered same-session safebox cell into an accepting carried destination (empty cell or compatible unlocked same-`vnum` stack under template `max_count`), removes that safebox cell, places/merges into inventory while preserving item identity on fresh-cell placement, persists the inventory/quickslot account snapshot, and emits self-only `SAFEBOX_DEL` plus inventory `ITEM_SET` / `ITEM_UPDATE` without deleting or retargeting carried-inventory item quickslots (closing an active merchant window and/or exchange shell first when present); closed presentation, empty/out-of-range source, and incompatible destinations stay fail-closed with no frames |
| `SAFEBOX_ITEM_MOVE` | client -> server | `0x0822` | game | documented | first owned safebox internal move ingress; payload is source packed `TItemPos`, destination packed `TItemPos`, and `count uint8`; `internal/game` decodes it in `GAME` through an explicit fail-closed-by-default handler seam; the shipped minimal runtime now accepts whole-stack same-session in-memory relocate / compatible merge and partial-count empty-destination split / compatible partial merge while `/open_safebox` is open (both positions may use TMP4 `WindowInventory` or explicit `WindowSafebox` and cells are interpreted as safebox slots inside `size * 5` capacity; whole-stack `count` is `0` or the full source count and emits self-only `SAFEBOX_DEL` + `SAFEBOX_SET`; partial `count` in `1..source_count-1` allocates a new destination item identity for empty cells or merges into a compatible destination and emits dual self-only `SAFEBOX_SET`; success may close an active merchant/exchange shell first) without mutating carried inventory/equipment, quickslots, gold, mall, or account persistence; closed presentation, out-of-range/same-cell/unsupported windows, oversize counts, identity-allocation overflow, and incompatible destinations stay fail-closed with no frames |
| `MALL_CHECKOUT` | client -> server | `0x0840` | game | documented | first owned mall checkout ingress; payload is `mall_pos uint8` plus packed `TItemPos`; `internal/game` decodes it in `GAME` through an explicit fail-closed-by-default handler seam, while the shipped minimal runtime rejects it fail-closed with no mall, carried inventory/equipment, quickslot, point, gold, ground-handle, peer, or persistence mutation until a later storage slice owns accepted semantics |
| `SAFEBOX_SET` | server -> client | `0x0830` | game | documented | first owned safebox item refresh codec; payload reuses the owned `ITEM_SET` fixed slot-refresh shape under the safebox header (`TItemPos`, `vnum`, `count`, flags, anti-flags, highlight, three sockets, seven attributes); the shipped minimal runtime emits it for accepted open-presentation check-in, reopen remembered rows, and accepted same-session item-move destination refresh |
| `SAFEBOX_DEL` | server -> client | `0x0831` | game | documented | first owned safebox slot-clear codec; payload reuses the owned `ITEM_DEL` packed `TItemPos` shape under the safebox header; the shipped minimal runtime emits it for accepted open-presentation check-out and accepted same-session item-move source clear |
| `SAFEBOX_WRONG_PASSWORD` | server -> client | `0x0832` | game | documented | first owned safebox password-failure status frame; header-only with no payload; the shipped minimal runtime emits it for warehouse `/safebox_password` mismatch against the durable effective password (blank/missing defaults to `000000`) after `ShowMeSafeboxPassword`, clears the pending challenge, and leaves presentation closed; malformed / missing / over-6-char passwords use info-chat instead; slash `/open_safebox` remains the no-password lab harness; durable `/safebox_change_password` uses info-chat only and does not emit this frame |
| `SAFEBOX_SIZE` | server -> client | `0x0833` | game | documented | first owned safebox size status codec; payload is `size uint8`; bootstrap `/open_safebox` no-password lab opener and matching `/safebox_password` success emit this frame (followed by in-range `SAFEBOX_SET` and `SAFEBOX_MONEY_CHANGE`) and remember a same-socket open flag for exchange busy-window policy for in-range sizes `1..3`; warehouse `open_safebox` `INTERACT` only prompts with `ShowMeSafeboxPassword` until password success; `/close_safebox` / `/safebox_close` clear that flag with self-only `CHAT_TYPE_COMMAND` `CloseSafebox`, and the same companion is also emitted when practice-mob floor, exact-position transfer / warp rebootstrap, or `/phase_select` / `/quit` / `/logout` clears an open presentation; out-of-range / invalid `/open_safebox` attempts stay fail-closed with no frame and no open-flag mutation; `/safebox_change_password` mutates durable password without emitting this frame; money / mall / client `SAFEBOX_CHANGE_PASSWORD` packets stay deferred |
| `SAFEBOX_MONEY_CHANGE` | server -> client | `0x0834` | game | documented | first owned safebox money status codec; payload is signed `money int32 LE`; bootstrap `/open_safebox` and matching `/safebox_password` success emit self-only after `SAFEBOX_SIZE` (+ SET) with durable warehouse gold (default `0`); `/safebox_money_save` / `/safebox_money_withdraw` emit it for the new warehouse total beside gold `PLAYER_POINT_CHANGE`; closed/pending-only paths emit no money frame; TMP4 CG `SAFEBOX_MONEY` request header and mall money stay deferred |
| `MALL_OPEN` | server -> client | `0x0841` | game | documented | first owned mall open status codec; payload is `size uint8`; codec-only for now, with mall open/runtime state still deferred |
| `MALL_SET` | server -> client | `0x0842` | game | documented | first owned mall item refresh codec; payload reuses the owned `ITEM_SET` fixed slot-refresh shape under the mall header; codec-only for now, and the shipped minimal runtime still emits no mall storage response frames for unsupported storage attempts |
| `MALL_DEL` | server -> client | `0x0843` | game | documented | first owned mall slot-clear codec; payload reuses the owned `ITEM_DEL` packed `TItemPos` shape under the mall header; codec-only for now, with accepted mall checkout/storage mutation semantics deferred |
| `SPECIAL_EFFECT` | server -> client | `0x0A30` | game | documented | first owned visual-effect companion; payload is `type uint8` plus `vid uint32`; current runtime emits it self-only after accepted direct carried consumable `ITEM_USE` when the resolved template authors non-zero `use_effect.special_effect_type`, after point/item/quickslot refresh frames and before the placeholder info chat; omitted/zero metadata preserves the older item-use burst |
| `ITEM_OWNERSHIP` | server -> client | `0x0517` | game | documented | first owned ownership-label companion for bootstrap ground items; payload is `vid uint32` plus fixed `owner_name[25]`; current runtime emits it immediately after every `ITEM_GROUND_ADD` for accepted drops, visible-peer drop fanout, radius-AOI ground re-entry rebuilds, and transfer destination ground re-entry rebuilds, and emits one blank `owner_name` frame when the bootstrap exclusive ownership window elapses; party ownership policy and restart-restored timers remain deferred |
| `ITEM_GET` | server -> client | `0x0518` | game | documented | first owned pickup-notice companion; payload is `vnum uint32`, `count uint8`, `arg uint8`, and fixed `from_name[25]`; current runtime emits normal/self `arg = 0` after accepted owner pickup, including stack-merge pickups, and the first party-shaped owner-delivery notices for visible live peers, while real party membership remains deferred |
| `ITEM_SET` | server -> client | `0x0511` | game bootstrap / game | documented | first owned self-only occupied-slot bootstrap/update family for carried inventory and equipped items; total frame length `54`; position is packed `TItemPos(window:uint8, cell:uint16)` and equipped items currently ride the legacy combined inventory/equipment cell namespace (`window = INVENTORY`, `cell = 90 + wear_index`); current runtime bootstrap/update emissions project owned item-template flags (`refineable`, `save`, `stackable`, `sell_count_per_gold`, `slow_query`, `rare`, `unique`, `make_count`, `irremovable`, `confirm_when_use`, `quest_use`, `quest_use_multiple`, `log`, `applicable`) into the packet `flags` field and owned anti-flags (`female`, `male`, job flags, empire flags, `get`, `drop`, `sell`, `give`, `stack`, `save`, `pk_drop`, `myshop`, `safebox`) into the packet `anti_flags` field while leaving unowned flag and anti-flag bits zero, and now project authored `highlight`, `sockets`, and `attributes` metadata into the fixed display fields; count-only carried-stack refreshes should prefer `ITEM_UPDATE` once the cell is already known |
| `ITEM_DEL` | server -> client | `0x0510` | game | documented | self-only slot-clear/remove companion for carried/equipped item mutations; total frame length `7` and payload is only packed `TItemPos`; the first consumable-use vertical also reuses it when consuming the last item removes that carried-slot stack entirely |
| `ITEM_UPDATE` | server -> client | `0x0514` | game | documented | count/socket/attribute refresh for an already-known item cell; total frame length `41`; payload is packed `TItemPos` + `count` + three sockets + seven attributes, with no `vnum`, flags, anti-flags, or highlight; current partial-stack `ITEM_USE`, compatible stack merges from packet/slash merchant buy, `ITEM_USE_TO_ITEM`, carried `ITEM_MOVE`, pickup merges into already-known compatible carried stacks, merchant `SHOP SELL2` partial-stack success, and counted `ITEM_DROP2` success paths emit it for changed carried cells while preserving loaded template-authored display socket/attribute arrays when template metadata is available |
| `ITEM_GROUND_ADD` | server -> client | `0x0515` | game | documented | first frozen ground-item spawn codec; payload is signed `x/y/z int32` coordinates followed by `vid uint32` and `vnum uint32`, matching the current TMP4-compatible client-facing struct order; current runtime use is after accepted carried-slot drops, visible-peer drop fanout, and radius-AOI move/sync re-entry rebuilds for pending bootstrap handles |
| `ITEM_GROUND_DEL` | server -> client | `0x0516` | game | documented | first frozen ground-item remove codec; payload is `vid uint32`; current runtime use is accepted pickup fanout, radius-AOI move/sync exit teardown, owner-session cleanup, and the bootstrap `300`-second in-memory destroy deadline for unclaimed pending handles |
| `QUICKSLOT_ADD` | client -> server | `0x0509` | game | documented | first owned client quickslot add codec and `GAME` dispatch seam; payload is `pos uint8` plus two-byte quickslot tuple `{type uint8, pos uint8}`; minimal runtime accepts valid selected-character edits, persists the quickslot snapshot, returns self-only refresh frames, rejects item bindings to empty, malformed, locked, duplicate-occupied, authored-missing-template, authored-mismatched-template, or over-template-max carried cells, deletes the older same-type tuple binding before adding a retargeted item/skill/command quickslot, treats `type = none` plus `pos = 0` as a clear request, rejects non-zero type-none payloads without clearing that bar position, and closes an active same-socket bootstrap exchange shell before accepted add refreshes |
| `QUICKSLOT_DEL` | client -> server | `0x050A` | game | documented | first owned client quickslot clear codec and `GAME` dispatch seam; payload is `pos uint8`; minimal runtime accepts valid selected-character deletes, persists the quickslot snapshot, returns self-only `GC::QUICKSLOT_DEL`, and closes an active same-socket bootstrap exchange shell before accepted delete refreshes |
| `QUICKSLOT_SWAP` | client -> server | `0x050B` | game | documented | first owned client quickslot swap codec and `GAME` dispatch seam; payload is `pos uint8` + `pos_to uint8`; minimal runtime accepts valid selected-character swaps, persists the quickslot snapshot, returns self-only `GC::QUICKSLOT_SWAP`, and closes an active same-socket bootstrap exchange shell before accepted swap refreshes |
| `QUICKSLOT_ADD` | server -> client | `0x0519` | game/bootstrap | documented | first owned server quickslot refresh codec; payload is `pos uint8` plus two-byte quickslot tuple `{type uint8, pos uint8}`; selected-character `ENTERGAME` bootstrap replays persisted quickslots as sorted self-only `QUICKSLOT_ADD` frames after the selected-character presence/state burst, and runtime `CG::QUICKSLOT_ADD` echoes the accepted self-only refresh |
| `QUICKSLOT_DEL` | server -> client | `0x051A` | game/bootstrap | documented | runtime `CG::QUICKSLOT_DEL` echoes the accepted self-only clear refresh; current item-mutation paths also emit it for removed item quickslots after whole-stack carried drops, last-stack consumable use, full-source `ITEM_USE_TO_ITEM` stack consolidation, whole-stack merchant sell, `probability = 100` refine-confirm material removal, `probability = 0` / failed `1..99` injected-roll refine-confirm material and destroyed-source removal, mutual-accept exchange finalize source removal, and accepted open-presentation `SAFEBOX_CHECKIN` when the carried source cell is fully cleared |
| `QUICKSLOT_SWAP` | server -> client | `0x051B` | game/bootstrap | documented | first owned server quickslot swap codec; payload is `pos uint8` + `pos_to uint8`; runtime `CG::QUICKSLOT_SWAP` echoes the accepted self-only swap refresh |

## Combat (bootstrap)

| Name | Direction | Header | Phase | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `TARGET` | client -> server | `0x0A01` | game | documented | first owned combat-preparation request for selecting one currently visible bootstrap combat actor by visible `vid`; payload is little-endian `uint32 target_vid`; `target_vid = 0` is the silent client-originated clear-target request and clears the current session's active target/cadence/engagement state without a self echo; the current live slice routes non-zero targets through the shared-world `AttemptStaticActorCombatTarget(...)` seam, accepts visible in-range `training_dummy`, authored `practice_mob`, and registered bootstrap combat-profile actors, and fails closed for malformed payloads, out-of-range targets, invisible targets, dead targets, non-targetable actors, aggro-engaged targets (`target_engaged` in runtime diagnostics), stale owners, and owners already at the bootstrap HP floor |
| `TARGET` | server -> client | `0x0A10` | game | documented | minimal self-only target-state acknowledgement; payload is little-endian `uint32 target_vid` + `uint8 hp_percent`; the current bootstrap combat path returns the runtime-owned current `hp_percent` on accepted selection, reuses the same family for accepted attack-driven HP refreshes, and freezes `target_vid = 0`, `hp_percent = 0` as the first clear-target companion for visibility/range invalidation, reconnect/rebootstrap cleanup, dummy/practice-mob death, third-party preselection clear on aggro-lite ownership, and the first retaliation-owned player death floor |
| `TARGET_UPDATE` | server -> client | `0x0A11` | game | documented | first target-marker location update codec; payload is `id int32`, `x int32`, `y int32`; currently codec/documentation only and deliberately separate from selected-combat-target HP refreshes |
| `TARGET_DELETE` | server -> client | `0x0A12` | game | documented | first target-marker removal codec; payload is `id int32`; currently codec/documentation only and not emitted by the shipped runtime |
| `TARGET_CREATE_NEW` | server -> client | `0x0A13` | game | documented | first target-marker presentation codec; payload is `id int32`, fixed `target_name[33]`, `vid uint32`, and `type uint8` with currently owned labels `none`, `location`, and `character`; currently codec/documentation only and not emitted by bootstrap combat, quest, NPC, map, death, restart, or reward runtime paths |
| `ON_CLICK` | client -> server | `0x0A02` | game | documented | first target/UI click ingress guard; payload is little-endian `uint32 vid`; `internal/game` decodes and dispatches it, while the shipped minimal runtime leaves NPC/shop/quest click gameplay unsupported and fail-closed with no self frames, no queued peer frames, no selected-target/HP mutation, no cadence/retaliation changes, no interaction/shop/quest side effects, and no point/inventory/persistence side effects |
| `ATTACK` | client -> server | `0x0401` | game | documented | first owned attack-intent family after accepted target selection; exact payload is `uint8 attack_type`, little-endian `uint32 target_vid`, `uint8 crc_proc_piece`, `uint8 crc_file_piece`; the current game-flow dispatcher rejects unsupported `attack_type != 0` silently before invoking runtime combat handlers, while the live bootstrap path accepts only `attack_type = 0`, requires the request `target_vid` to match the session's active selected target exactly, applies one fixed `250ms` normal-attack cadence window after each accepted hit, keeps accepted retargets from resetting that window so an immediate attack against a newly selected second target still fails closed until it expires, mutates runtime-owned bootstrap combat HP through the current combat-profile defaults (`max_hp`, attack/defense formula, safely bounded respawn delay, optional death reward), reuses self-only `TARGET(target_vid, hp_percent)` for non-lethal hit refreshes, and transitions the zero-HP edge to `DEAD(vid)` plus target clear before appending any owned reward feedback |
| `USE_SKILL` | client -> server | `0x0402` | game | documented | first skill-intent ingress guard; payload is little-endian `uint32 skill_vnum` plus little-endian `uint32 target_vid`; `internal/game` decodes and dispatches it, while the shipped minimal runtime leaves skill combat unsupported and fail-closed with no self frames, no queued peer frames, no target HP mutation, no cadence/retaliation changes, and no point/inventory/persistence side effects |
| `SHOOT` | client -> server | `0x0403` | game | documented | first ranged-shot ingress guard; payload is `uint8 shoot_type`; the current shipped runtime decodes it but keeps ranged/projectile/skill behavior unsupported and fail-closed with no self frames, no queued peer frames, no target HP mutation, and no normal-attack cadence change |
| `FLY_TARGETING` | client -> server | `0x0404` | game | documented | first projectile-targeting ingress guard; payload is little-endian `uint32 target_vid`, signed `int32 x`, signed `int32 y`; the current shipped runtime decodes it but keeps projectile/fly-effect/skill behavior unsupported and fail-closed with no self frames, no queued peer frames, no target HP mutation, no cadence/retaliation changes, and no point/inventory/persistence side effects |
| `ADD_FLY_TARGETING` | client -> server | `0x0405` | game | documented | same fixed-width projectile-targeting payload as `FLY_TARGETING`; currently owned only as a decode-and-fail-closed guard for additional fly targets/positions, not as multi-target skill or projectile gameplay |
| `DAMAGE_INFO` | server -> client | `0x0410` | game | documented | first hit-effect carrier; payload is little-endian `uint32 vid`, raw `uint8 flag`, and little-endian signed `int32 damage`; standalone bootstrap combat-profile non-lethal normal hits append one self plain-flag `DAMAGE_INFO` immediately after the authoritative self `TARGET` HP refresh and queue the same hit-effect companion to currently visible live peers, using the shared-world damage descriptor for custom registered formula profiles; content-loaded spawn-backed practice mobs now append one owner self-only plain-flag mob `DAMAGE_INFO` after the existing immediate retaliation `PLAYER_POINT_CHANGE` on accepted non-lethal owner-surviving hits, then one owner self-only plain-flag retaliation `DAMAGE_INFO(owner_vid, abs(delta))`, and queue both the mob and owner retaliation plain hit-effect frames to currently visible live peers; non-floor delayed retaliation beats also append that owner self-only retaliation companion after their point-change and queue the same owner companion to currently visible live peers; killing hits, owner-floor beats, broader hit-result policy, and richer flags remain later policy |
| `FLY_TARGETING` | server -> client | `0x0411` | game | documented | first server fly-targeting presentation codec; payload is `shooter_vid uint32`, `target_vid uint32`, `x int32`, `y int32`; currently codec/documentation only and not emitted by bootstrap `ATTACK`, `SHOOT`, `USE_SKILL`, or retaliation paths |
| `ADD_FLY_TARGETING` | server -> client | `0x0412` | game | documented | first additional server fly-targeting presentation codec with the same fixed payload as `FLY_TARGETING`; currently codec/documentation only and not emitted by the shipped runtime |
| `CREATE_FLY` | server -> client | `0x0413` | game | documented | first server fly-effect creation codec; payload is `type uint8`, `start_vid uint32`, `end_vid uint32`; projectile visual type meanings, hit timing, and runtime emission remain later ranged/skill policy |
| `PVP` | server -> client | `0x0414` | game | documented | first PVP relation presentation codec; payload is `source_vid uint32`, `destination_vid uint32`, `mode uint8` with currently owned mode labels `none`, `agree`, `fight`, and `revenge`; currently codec/documentation only and not emitted by bootstrap PvE combat, death, restart, or social paths |
| `DUEL_START` | server -> client | `0x0415` | game | documented | first duel-start presentation codec; payload is a variable list of little-endian `uint32 opponent_vid` entries and may be empty; currently codec/documentation only and not emitted by the shipped runtime |
| `DEAD` | server -> client | `0x0217` | game | documented | first visible zero-HP death signal family reused by both the bootstrap `training_dummy` and the first practice-mob retaliation-owned player death floor; payload is little-endian `uint32 vid`; dummy death fanout is limited to sessions that can currently see that actor, and sessions that still had it selected pair that death edge with the existing self-only `TARGET(0, 0)` clear companion, while the player-death floor currently uses self `DEAD(owner_vid)` + self `TARGET(0, 0)`, one visibility-gated queued peer `DEAD(owner_vid)`, and self `DEAD(owner_vid)` replay during fresh `ENTERGAME` for an already-`0`-HP selected-character snapshot without broader corpse/respawn choreography |
| `CHARACTER_POSITION` | client -> server | `0x0A60` | game | documented | first position / battle-position ingress; payload is `uint8 position`; the current shipped runtime accepts `position=0` stand/general, `position=4` ground-sit, and `position=3` chair-sit normalized to the owned ground-sit presentation as presentation-only requests while the selected character is live and above the bootstrap zero-HP floor, returning self `GC CHARACTER_POSITION(selected_vid, position)` and queuing the same frame to visible live peers only when the normalized stance differs from the current session state; duplicate stand/sit requests accept as no-op with no repeated frame, zero-HP owners fail closed before any self/peer stance frame, and unsupported bytes, including the current battle-position byte, still fail closed with no frames, no selected-target or HP mutation, no cadence/retaliation changes, and no side effects |

## Notes

- This matrix is a working contract, not a dump of every observed packet family.
- Packets remain `candidate` until tests and captures freeze the final path.
- The first implementation should only touch the rows needed for the boot path milestone.
