# Playable PvE Vertical Roadmap — 2026-08-08

This roadmap replaces the May 2026 broad parity map as the current planning anchor. The project should still move toward legacy parity, but the next useful milestone is narrower: make the server feel like a small, inspectable, recoverable PvE game loop.

This is not permission for a large rewrite. Keep landing small, verified slices and keep `main` green.

## Guiding rules

1. Keep `main` green and linear.
2. Keep one cohesive commit per slice.
3. Keep docs/specs/tests aligned with behavior.
4. Treat legacy source and captures only as external behavior oracles.
5. Prefer client-visible behavior over generic refactors.
6. Prefer runtime behavior that can be inspected by local ops endpoints while the system is still bootstrap-shaped.
7. Do not claim full legacy parity when a slice is only a practice-mob, file-store, or local-debug seam.

## Milestone goal

A narrow playable vertical means a player can:

1. connect through `authd` and `gamed`,
2. enter a map,
3. see authored NPC/static content and content-loaded mobs,
4. move and keep visibility stable,
5. target and fight mobs,
6. kill mobs and receive deterministic rewards,
7. pick up, use, equip, trade/reject, or sell basic items through owned paths,
8. reconnect/restart without corrupting state,
9. let an operator inspect and recover bootstrap stores through local-only tools.

Exit criteria for this milestone:

- A manual client QA pass can exercise login → map → mob kill → reward → pickup/use/equip/shop → reconnect/restart without relying primarily on slash/debug paths.
- Automated tests cover the same contract at protocol, runtime, and persistence boundaries.
- Unsupported behaviors fail closed and are documented.
- Local operator endpoints can validate the relevant state before and after the run.

## Track A — Mob AI and spawn lifecycle

**Objective:** turn stationary practice spawns into the first believable PvE mob lifecycle.

Primary lane:

- `lane/world` / `go-metin2-mob-spawn-lifecycle-worker`

Likely areas:

- `internal/worldruntime`
- `internal/minimal`
- `internal/contentbundle`
- `internal/staticstore`
- `spec/protocol/non-player-entity-bootstrap.md`
- `spec/protocol/content-spawn-groups-bootstrap.md`
- `spec/protocol/combat-normal-attack-bootstrap.md`
- `docs/qa/manual-client-checklist.md`

Next slices:

1. ~~Finish integrating map-local static actor snapshot coverage already pending on `lane/world`.~~ Done: `StaticActorsForMap` + loopback `GET /local/maps/{map_index}/static-actors` are owned with focused runtime and ops coverage.
2. ~~Make one content-loaded practice mob lifecycle fully explicit: spawn → target → hit → death → respawn → fresh reselection.~~ Done for bootstrap scope: `TestGameSessionFlowContentSpawnGroupPracticeMobRespawnsAfterServerDrivenDelayAndRequiresFreshReselect` owns the content-loaded kill → server-driven respawn → fresh reselection contract.
3. ~~Add aggro-lite reset/cleanup boundaries for disconnect, transfer, death, and respawn.~~ Done for bootstrap scope: leave/logout/close, phase-select, transfer/rebootstrap, owner death floor, actor death/respawn, return-home/return-step, operator update, and EnterGame reclaim (including chase-deadline prune) release engagement / selected-target / pending retaliation ownership; `/restart_here` also has focused coverage for due return-step and due chase-step preflight while a zero-HP owner skipped lifecycle frames, and `/restart_town` now mirrors due destination respawn / return-step / chase-step / homeward-step preflights (return-step via outside-leash displace; chase-step with a live destination engager; homeward-step after chase→release on the destination map).
4. ~~Add first independent mob reaction timing that is not only piggybacked on player hits.~~ Done for bootstrap scope: proximity aggro-radius acquisition has the pure helper plus pending-frame live consumer, arms delayed self-only server-origin retaliation and chase without inventing selected-target ownership, releases on leave-radius walk-away, and keeps the same still-inside candidate suppressed until leave `DefaultSpawnAggroRadius` + re-enter after in-radius release as well as after death/respawn seed.
5. ~~Extend the first chase/leash/return planning seam beyond pure classification: tested pure return-step and chase-step planners, the pending-frame chase executor (including proximity-armed due chase without a hit), read-only pending chase inspection, retained-viewer chase `MOVE` replication, retained-viewer same-map return-step / return-home `MOVE` replication, and retained-viewer same-map live spawn-backed operator/runtime position `MOVE` now exist; presentation/name/race refreshes, cross-map return-home, and respawn rebuild remain on delete/readd.~~
6. ~~Harden multi-map and reconnect behavior so mobs do not duplicate, leak, or resurrect incorrectly.~~ Done for bootstrap scope: content-loaded still-dead EnterGame/reconnect now mirrors the `training_dummy` trailing-`DEAD` proof, due-respawn EnterGame preflight stays owned, one-ref/one-actor lookup fails closed on duplicates, import visibility stays map/AOI scoped, cross-map return-home delete/readd dual-map occupancy coverage is owned, automatic pending-frame cross-map return-step after `UpdateStaticActor` displace now mirrors that same dual-map anti-leak proof (due flush snaps to authored home via delete/readd with no invented MOVE), Leave/transfer ownership cleanup remains owned, and non-identical still-dead content-bundle replacement remaps pending dead/respawn state by authored `spawn_group_ref` instead of resurrecting early. Same-map live spawn-backed operator/runtime position updates now reuse retained-viewer `MOVE` (presentation/name/race stay on delete/readd). Daemon-restart still-dead timer persistence is now owned too: spawn-backed static-actor snapshots carry optional `combat_current_hp=0` plus absolute `respawn_ready_at`, process restart rematerializes mid-dead practice mobs as still-dead / non-targetable through the remaining deadline, and successful respawn clears those persistence fields. Daemon-restart live damaged HP persistence is now owned beside that seam: accepted non-lethal spawn-group hits persist `combat_current_hp` in `1..max_hp-1` with `respawn_ready_at` omitted, process restart rematerializes the damaged HP / `hp_percent`, and engagement / selected-target / chase / return ownership stay fail-closed across restart. Live damaged HP remapping across non-identical content-bundle replacement is now owned beside still-dead replacement remapping (engagement stays fail-closed). Cross-map return MOVE / warp choreography remains deferred behind the `spawn-leash-bootstrap.md` packet freeze (delete/readd + dual-map occupancy stay owned today).
7. ~~Next: profile-authored optional `aggro_radius` on portable `combat_profiles`.~~ Done for bootstrap scope: optional authored `aggro_radius` round-trips through `combat_profiles`, resolves through `EffectiveStaticActorSpawnAggroRadius` / `...ForActor`, and live proximity acquire / leave-radius release / suppress seeding honor the effective radius (default `200`, fail-closed above the profile's effective leash).
8. ~~Next: profile-authored optional `leash_radius` on portable `combat_profiles`.~~ Done for bootstrap scope: optional authored `leash_radius` round-trips through `combat_profiles`, resolves through `EffectiveStaticActorSpawnLeashRadius` / `...ForActor`, and live leash classification / return-step / chase clamp / `target_return_required` gating honor the effective radius (default `400`, fail-closed below the profile's effective aggro). Operator leash GET endpoints keep an explicit `radius` override and default omitted lookups through the actor effective leash. Cross-map return MOVE / warp choreography remains deferred behind the packet freeze.
9. ~~Next: unengaged `within_radius` homeward-step after chase/engagement release.~~ Done for bootstrap scope: pure `PlanStaticActorSpawnLeashHomewardStep`, pending-frame `1s` / `max_step=100` homeward executor with same-map retained-viewer `MOVE`, engagement-release arming, chase mutual exclusion, EnterGame/transfer/restart preflight flush after `return_required` return-steps / before chase, focused EnterGame / MOVE-transfer / `/restart_here` / `/restart_town` due-homeward preflight coverage now owned alongside chase/return, daemon-restart rematerialization of live unengaged `within_radius` spawn-backed actors now arms pending homeward the same way `return_required` arms return-step, and read-only pending homeward inspection (`/local/spawn-group-homeward-steps`, exact, map-local). Operator `return-step` still no-ops `within_radius`; exact-home remains `return-home`. Operator/runtime `UpdateStaticActor` that leaves an unengaged spawn-backed actor `within_radius` now re-arms pending homeward through the shared eligibility sync. Cross-map return MOVE stays deferred.
10. ~~Next: operator/runtime same-map position `UpdateStaticActor` that leaves a live unengaged spawn-backed actor classified `within_radius` should re-arm pending homeward (mirroring how `return_required` re-arms return-step) instead of only clearing the deadline.~~ Done for bootstrap scope: `UpdateStaticActor` now calls `syncSpawnGroupHomewardStepScheduleForEntity` after return-step sync / chase clear; focused coverage proves arming plus due homeward flush restoring `at_home` without arming return-step.
11. ~~Next: automatic pending-frame cross-map return-step dual-map anti-leak after `UpdateStaticActor` displace.~~ Done for bootstrap scope: `TestGameRuntimeFlushServerFramesAppliesDueCrossMapSpawnGroupReturnStepLeavesNoDualMapOccupancy` owns arm → due flush → authored-home delete/readd with no invented MOVE, one-ref/one-actor, empty foreign-map occupancy, cleared pending schedule, and persisted authored home beside the already-owned operator return-home dual-map proof.
12. ~~Next: content-bundle import prune/restore for pending homeward-step schedules (mirror return/chase).~~ Done for bootstrap scope: `ImportContentBundle` no-op / successful replacement / failed-rollback paths now prune or restore `spawnHomewardStepDueAt` beside return/chase; focused coverage owns stale prune, removed-actor clear, and failed-import due-at restore + retained-viewer homeward `MOVE`.
13. ~~Next: keep within-radius homeward armed across owner death-floor engagement release after chase displace.~~ Done for bootstrap scope: immediate and delayed death-floor paths no longer clear the homeward schedule that `clearActiveCombatTarget` just armed; `TestGameRuntimeOwnerDeathFloorArmsHomewardAfterChaseDisplaceWithinRadius` proves the still-connected dead owner is skipped for retained homeward `MOVE` while a living watcher receives the due step back to authored home.
14. ~~Next: proximity-armed owner death-floor release must seed leave/re-enter suppress through same-socket `/restart_here` while still inside aggro radius.~~ Done for bootstrap scope: `TestGameRuntimeProximityAggroSuppressesReacquireUntilLeaveAndReenterAfterOwnerDeathFloorRestartHere` proves the recovered still-inside owner stays suppressed through later pending-frame flushes until an explicit leave/re-enter of `DefaultSpawnAggroRadius`, matching `TARGET(0)` and mob death/respawn suppress seeding.
15. ~~Next: proximity suppress must survive `/phase_select` Leave → fresh Join (new entity ID) → `/restart_here` while still inside radius.~~ Done for bootstrap scope: Leave parks subject suppress under character VID and Join rematerializes onto the new entity ID; focused coverage owns both `/phase_select` (`TestGameRuntimeProximityAggroSuppressesReacquireUntilLeaveAndReenterAfterOwnerDeathFloorPhaseSelectRestartHere`) and abrupt reconnect (`TestGameRuntimeProximityAggroSuppressesReacquireUntilLeaveAndReenterAfterOwnerDeathFloorReconnectRestartHere`). Subject-release suppress seeding for an already-floored shared-world owner snapshot is owned by Track B (`TestSharedWorldRegistrySubjectReleaseSeedsProximitySuppressWhenOwnerAlreadyAtHPFloor`).
16. ~~Next: freeze cross-map return client choreography instead of leaving a speculative MOVE/WARP follow-on open.~~ Done for bootstrap scope: `spawn-leash-bootstrap.md` now freezes cross-map return-home / return-step as the already-owned delete/readd / direct-home rebuild path (`CHARACTER_DEL` on origin, ordinary add/info/update on home, one-ref/one-actor, no invented cross-map `MOVE` / `GC WARP`). Legacy evidence keeps `WarpSet` / `TPacketGCWarp` PC-only and aligns mob map/sectree relocation with Show remove/insert rather than WARP. Speculative cross-map MOVE/WARP RED is cancelled for Track A bootstrap; further Track A work needs a new evidence-backed seam beyond this freeze.
17. ~~Next: hermetic MemoryStores for proximity aggro suppress shared-world suites.~~ Done for bootstrap scope: the six live proximity suppress proofs in `proximity_aggro_suppress_test.go` now inject `staticstore.NewMemoryStore()` / `interactionstore.NewMemoryStore()` instead of disposable content FileStores; login-ticket / account FileStores remain intentional. See [hermetic proximity aggro suppress MemoryStore tests](2026-08-24-hermetic-proximity-aggro-suppress-memorystore-tests.md).
18. ~~Next: freeze daemon-restart proximity-suppress rematerialization before RED.~~ Done for bootstrap scope: docs-first freeze landed first, then GREEN `TestGameRuntimeProximityAggroSuppressRematerializesAcrossDaemonRestart` owns save/restore of optional `proximity_suppress_vids` through the static-actor snapshot into the existing Leave→Join VID park/claim handoff. Engagement stays fail-closed across restart; leave/re-enter still clears rematerialized suppress. See [proximity suppress daemon-restart rematerialize contract freeze](2026-08-24-proximity-suppress-daemon-restart-rematerialize-contract-freeze.md).
19. ~~Next: prove same-map spawn MOVE AOI remove/add membership beside retained-viewer MOVE.~~ Done for bootstrap scope: `TestGameRuntimeUpdateStaticActorSameMapSpawnGroupPositionQueuesOldPositionOnlyDeleteAndNewlyVisibleAdd` owns old-position-only `CHARACTER_DEL`, retained midway `MOVE`, and newly-visible destination add/info/update for live same-map spawn-backed operator/runtime position updates. Chase / return / homeward already share `RelocateStaticActorTargetDiff`; absolute chase/return/homeward deadline persistence across daemon restart stays deferred (re-arm-from-now). Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [spawn MOVE AOI membership](2026-08-24-spawn-move-aoi-membership.md).
20. ~~Next: prove same-map chase / return / homeward MOVE AOI remove/add membership twins beside operator/runtime position MOVE.~~ Done for bootstrap scope: `TestGameRuntimeFlushServerFramesAppliesDueSpawnGroupChaseStepQueuesOldPositionOnlyDeleteAndNewlyVisibleAdd`, `…ReturnStep…`, and `…HomewardStep…` own old-position-only `CHARACTER_DEL`, retained midway `MOVE`, and newly-visible destination add/info/update for due same-map server-owned steps through the shared `RelocateStaticActorTargetDiff` path. Absolute chase/return/homeward deadline persistence across daemon restart stays deferred (re-arm-from-now). Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [spawn step AOI membership](2026-08-25-spawn-step-aoi-membership.md).
21. ~~Next: live leash-clamped chase clear + same-engagement hit re-arm.~~ Done for bootstrap scope: `TestGameRuntimeFlushServerFramesClearsLeashClampedSpawnGroupChaseStepAndRearmsOnHit` owns due chase MOVE to the effective leash boundary, pending-chase clear while engagement / selected-target stay preserved, no automatic follow-up while cleared, same-engagement accepted hit re-arm of the owned `5s` deadline, and a later due chase MOVE after the owner walks inward so the actor is again safely inside leash. Absolute chase/return/homeward deadline persistence across daemon restart stays deferred (re-arm-from-now). Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [spawn chase leash clamp rearm](2026-08-25-spawn-chase-leash-clamp-rearm.md).
22. ~~Next: freeze hit-armed vs proximity walk-away chase asymmetry before opening a twin proof.~~ Done for bootstrap scope (docs/spec freeze only): proximity-only leave-radius walk-away remains the owned release path (`TestGameRuntimeProximityAggroWalkAwayReleasesEngagementAndCancelsDelayedRetaliation`), while hit-armed / still-selected owners that walk outside aggro but stay inside combat-target range keep engagement + pending chase until an explicit release boundary. Focused twin coverage stays deferred as ordinary GREEN follow-on rather than dishonest RED against already-live gating. Absolute schedule rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [hit-armed chase walkaway asymmetry contract freeze](2026-08-25-hit-armed-chase-walkaway-asymmetry-contract-freeze.md).
23. ~~Next: GREEN twin proving hit-armed chase survives owner walk outside aggro.~~ Done for bootstrap scope: `TestGameRuntimeHitArmedSpawnGroupChaseSurvivesOwnerWalkOutsideAggroRadius` owns accepted-hit chase arm → walk outside `DefaultSpawnAggroRadius` while still inside combat-target range / leash / visibility → engagement + selected-target + pending chase preserved (no proximity leave-radius release) → delayed retaliation continues → due chase retained-viewer `MOVE` still fires. Absolute chase/return/homeward deadline persistence across daemon restart stays deferred (re-arm-from-now). Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [hit-armed chase walkaway twin](2026-08-25-hit-armed-chase-walkaway-twin.md).
24. ~~Next: GREEN twin proving multi-step homeward cadence when chase displace > `max_step=100`.~~ Done for bootstrap scope: `TestGameRuntimeFlushServerFramesAppliesMultiStepSpawnGroupHomewardCadenceAfterChaseDisplaceBeyondMaxStep` owns two chase beats to +200 from authored home → engagement release arms `1s` homeward → first due homeward `MOVE` +100 re-arms while still `within_radius` → second due lands on authored home / `at_home` and clears the deadline. Absolute chase/return/homeward deadline persistence across daemon restart stays deferred (re-arm-from-now). Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [multi-step homeward cadence twin](2026-08-25-multi-step-homeward-cadence-twin.md).
25. ~~Next: GREEN twin proving chase replan when the owner moves between arm and first due flush.~~ Done for bootstrap scope: `TestGameRuntimeFlushServerFramesReplansSpawnGroupChaseTowardOwnerMovedBetweenArmAndDue` owns accepted-hit chase arm near home → owner walk farther along the same axis while engagement / selected-target / pending chase stay armed → due chase retained-viewer `MOVE` plans toward the live post-move owner coords (not the arm-time snapshot). Absolute chase/return/homeward deadline persistence across daemon restart stays deferred (re-arm-from-now). Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [chase replan owner-moved twin](2026-08-25-chase-replan-owner-moved-between-arm-and-due.md).
26. ~~Next: freeze absolute chase/return/homeward deadline rematerialize across daemon restart instead of leaving a speculative absolute due-at follow-on open.~~ Done for bootstrap scope (docs/spec freeze only): keep already-live **re-arm-from-now** restore for eligible return/homeward on `loadPersistedStaticActors`; chase stays unarmed across restart until fresh post-restart engagement (engagement / selected-target stay fail-closed). Speculative absolute mid-timer due-at rematerialize RED is cancelled for Track A bootstrap; further Track A work needs a new evidence-backed seam beyond this freeze. Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [absolute chase/return/homeward deadline rematerialize contract freeze](2026-08-25-absolute-chase-return-homeward-deadline-rematerialize-contract-freeze.md).
27. ~~Next: optional ordinary GREEN composite twin asserting restart leaves chase unarmed beside the already-owned return/homeward restore-arm proofs.~~ Done for bootstrap scope: `TestGameRuntimeDaemonRestartRearmsReturnAndHomewardFromNowAndLeavesChaseUnarmed` owns one chase-displaced `within_radius` rematerialize that re-arms homeward from now, one `return_required` rematerialize that re-arms return-step from now, chase staying unarmed across restart for both, and engagement / selected-target ownership staying fail-closed. Absolute mid-timer due-at rematerialize RED remains cancelled. Cross-map MOVE/WARP and pack AI remain cancelled / out of scope. See [daemon-restart chase-unarmed re-arm-from-now twin](2026-08-25-daemon-restart-chase-unarmed-rearm-from-now-twin.md).
28. ~~Next: profile-authored chase arming delay (`combat_profiles.chase_delay_ms`).~~ Done for bootstrap scope: optional authored chase delay round-trips through portable combat profiles / snapshots / content-bundle / staticstore, migration `0016` adds `chase_delay_ms`, and live arming / re-arm consume `EffectiveStaticActorSpawnChaseDelay` (`TestGameRuntimeAuthoredChaseDelayArmsAndRearmsAtTwoSeconds`). Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored chase delay](2026-08-28-profile-authored-chase-delay-ms.md).
29. ~~Next: profile-authored return-step delay (`combat_profiles.return_delay_ms`).~~ Done for bootstrap scope: optional authored return delay round-trips through portable combat profiles / snapshots / content-bundle / staticstore, migration `0017` adds `return_delay_ms`, and live arming / re-arm consume `EffectiveStaticActorSpawnReturnDelay` (`TestGameRuntimeAuthoredReturnDelayArmsAndRearmsAtTwoSeconds`). Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored return delay](2026-08-28-profile-authored-return-delay-ms.md).
30. ~~Next: freeze optional profile-authored homeward-step delay (`homeward_delay_ms`) before RED.~~ Done for bootstrap scope (docs/spec freeze only): optional `combat_profiles.homeward_delay_ms` contract is frozen; live homeward arming stays on bootstrap `1s` until GREEN. Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored homeward delay freeze](2026-08-28-profile-authored-homeward-delay-ms.md).
31. ~~Next: profile-authored homeward-step delay (`combat_profiles.homeward_delay_ms`).~~ Done for bootstrap scope: optional authored homeward delay round-trips through portable combat profiles / snapshots / content-bundle / staticstore, migration `0018` adds `homeward_delay_ms`, and live arming / re-arm consume `EffectiveStaticActorSpawnHomewardDelay` (`TestGameRuntimeAuthoredHomewardDelayArmsAndRearmsAtTwoSeconds`). Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored homeward delay](2026-08-28-profile-authored-homeward-delay-ms.md).
32. ~~Next: freeze optional profile-authored spawn step cap (`max_step`) before RED.~~ Done for bootstrap scope (docs/spec freeze only): optional `combat_profiles.max_step` contract is frozen; live chase / return / homeward planners stay on bootstrap `100` until GREEN. Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored max step freeze](2026-08-28-profile-authored-max-step.md).
33. ~~Next Track A evidence-backed seam after the max-step freeze: RED → GREEN for profile-authored `max_step` (shared chase/return/homeward step cap; no speculative MOVE/WARP / pack AI / absolute due-at RED).~~ Done for bootstrap scope: optional authored `max_step` round-trips through portable combat profiles / snapshots / content-bundle / staticstore, migration `0019` adds `max_step`, live chase / return / homeward planning + due-step execution / pending inspection consume `EffectiveStaticActorSpawnMaxStep`, and operator `return-step` omit→effective uses the profile step (`TestGameRuntimeAuthoredMaxStepPlansHomewardAtFifty`). Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored max step](2026-08-28-profile-authored-max-step.md).
34. ~~Next Track A evidence-backed seam after owned `max_step`: freeze then land profile-authored independent mob reaction cadence (`reaction_delay_ms`) only with a docs/spec contract first — server-owned beat distinct from hit-piggyback and from already-owned chase/return/homeward delays; no speculative MOVE/WARP / pack AI / absolute due-at RED.~~ Done for bootstrap scope (docs/spec freeze only): optional `combat_profiles.reaction_delay_ms` contract is frozen; live delayed server-origin retaliation arming stays on bootstrap `1s` until GREEN. Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored reaction delay freeze](2026-08-29-profile-authored-reaction-delay-ms.md).
35. ~~Next Track A evidence-backed seam after the reaction-delay freeze: RED → GREEN for profile-authored `reaction_delay_ms` (delayed server-origin retaliation arming / re-arm; no speculative MOVE/WARP / pack AI / absolute due-at RED).~~ Done for bootstrap scope: optional authored `reaction_delay_ms` round-trips through portable combat profiles / snapshots / content-bundle / staticstore, migration `0020` adds `reaction_delay_ms`, and live delayed server-origin retaliation arming / re-arm consume `EffectiveStaticActorSpawnReactionDelay` (`TestGameRuntimeAuthoredReactionDelayArmsAndRearmsAtTwoSeconds`). Absolute due-at rematerialize / cross-map MOVE/WARP / pack AI remain cancelled / out of scope. See [profile-authored reaction delay](2026-08-29-profile-authored-reaction-delay-ms.md).
36. Next Track A evidence-backed seam after owned `reaction_delay_ms`: keep absolute due-at rematerialize / cross-map MOVE/WARP / pack AI cancelled; prefer reward-descriptor / player-death hardening elsewhere unless a new evidence-backed Track A timing seam appears.

Exit criteria:

- content-loaded mobs round-trip through authored data and runtime state,
- a dead mob stays dead until its server-owned respawn boundary,
- respawn uses the normal visibility rebuild path,
- transfer/reconnect cannot leave stale target or engagement ownership behind,
- docs keep calling this bootstrap/practice AI until movement/pathing is real.

Anti-goals:

- no full pathfinding rewrite,
- no broad formula work in the world lane,
- no distributed channel/shard architecture before single-process semantics are stable.

## Track B — Combat, death, rewards, and formulas

**Objective:** make the PvE loop resolve damage, death, and rewards through owned contracts.

Primary lane:

- `lane/combat` / `go-metin2-combat-worker`

Likely areas:

- `internal/proto/combat`
- `internal/proto/world`
- `internal/worldruntime`
- `internal/minimal`
- `internal/player`
- `spec/protocol/combat-*`
- `spec/protocol/non-player-death-respawn-bootstrap.md`
- `spec/protocol/non-player-reward-bootstrap.md`
- `spec/protocol/player-death-bootstrap.md`

Next slices:

1. Keep target-marker, fly-effect, stun, PvP/duel packet families codec-only until runtime policy is proven.
2. ~~Define one authored combat-profile formula seam for practice mobs without claiming full legacy math.~~ Done for bootstrap scope: portable `combat_profiles` already drive `max(1, attack_value - defense_value)`, and `docs/examples/bootstrap-combat-profile-formula-bundle.json` is the first playable QA fixture (`qa_formula_practice_mob` / `practice.qa_formula_mob`). The composed PvE vertical authoring fixture now also owns packet-level first-hit formula `DAMAGE_INFO(damage = 5)` coverage beside hit-count kill proofs. Full legacy math remains out of scope.
3. ~~Extend reward descriptors from fixed examples toward table-driven EXP/gold/drop data.~~ Done for bootstrap scope: authoring-only `drop_tables` + `reward_drop_table_ref` expand into fixed spawn-group EXP/gold/drop-vnum descriptors before import (`docs/examples/bootstrap-drop-table-authoring-bundle.json`); weighted/random loot tables and pickup mutation remain out of scope / items-lane. Kill-reward pending ground handles also rematerialize across `gamed` process restart from the dedicated ground-item FileStore with absolute ownership/despawn timers (`TestGameRuntimePracticeMobKillRewardDropRematerializesAcrossDaemonRestart`); SQL import/backfill from quarantined `0010` exports is now owned beside the other Track E import seams (`worldruntime.ImportBootstrapGroundItemState`).
4. Harden player-death floor and restart behavior around mob retaliation and reconnect: **done for bootstrap scope** — when immediate/delayed practice-mob retaliation reaches the `0`-HP floor, the selected-character account snapshot now persists that bootstrap HP point as `0`, so reconnect / `/phase_select` / fresh `ENTERGAME` replay dead bootstrap (`PLAYER_POINT_CHANGE` at `0` + `GC DEAD(owner_vid)`). Accepted `/restart_here` / `/restart_town` restore race create MaxHP into that persisted snapshot as part of recovery. Partial (above-floor) retaliation loss stays runtime-only. Proximity-armed delayed beats that never invented selected-target ownership now also prove the same floor ordering (`PLAYER_POINT_CHANGE(value=0)` → `DEAD` → `TARGET(0, 0)`), peer `DEAD` fanout, persistence, engagement release, and post-floor fail-closed gates via `TestGameRuntimeProximityAggroDelayedRetaliationReachesOwnerDeathFloorWithoutHitOrTarget`. Same-socket `/restart_here` while still inside aggro radius after that proximity-armed floor keeps leave/re-enter proximity suppress via `TestGameRuntimeProximityAggroSuppressesReacquireUntilLeaveAndReenterAfterOwnerDeathFloorRestartHere`. The same suppress now also survives `/phase_select` and abrupt reconnect Leave → Join identity changes through VID park/claim before `/restart_here` (`…PhaseSelectRestartHere` / `…ReconnectRestartHere`). Subject-side engagement release also marks the releasing owner for suppress when that owner's shared-world snapshot is already at the bootstrap `0`-HP floor (`TestSharedWorldRegistrySubjectReleaseSeedsProximitySuppressWhenOwnerAlreadyAtHPFloor`), so later live-HP recovery cannot silently drop the still-inside suppress gate. Non-identical same-`spawn_group_ref` content-bundle replacement now remaps proximity-suppress membership for still-connected subjects onto the newly registered actor (`TestGameRuntimeProximityAggroSuppressRemapsAcrossContentBundleReplacement`) while engagement stays fail-closed; daemon-restart suppress rematerialization is now GREEN (`TestGameRuntimeProximityAggroSuppressRematerializesAcrossDaemonRestart`; see Track A #18). Broader corpse / revive menus stay out of scope.
5. Add first accepted skill/ranged/projectile runtime only after packet evidence and tests are owned.
6. Defer PvP/duel runtime policy until the PvE baseline is stable.

Exit criteria:

- combat outcome is deterministic, tested, and visible to the client,
- rewards flow into item/gold/EXP systems through existing mutation/persistence seams,
- player death/restart does not corrupt inventory, quickslots, target state, or persistence,
- unsupported combat packet families fail closed instead of producing fake behavior.

Anti-goals:

- no full skill system before normal PvE combat is stable,
- no PvP policy guessing,
- no fake loot/PnL-style evidence; rewards must be backed by tests and docs.

## Track C — Items, economy, trade, and storage foundations

**Objective:** make item/economy behavior useful enough to support the PvE loop.

Primary lane:

- `lane/items` / `go-metin2-items-worker`

Likely areas:

- `internal/player`
- `internal/inventory`
- `internal/itemstore`
- `internal/proto/item`
- `internal/proto/quickslot`
- `internal/minimal`
- `spec/protocol/item-*`
- `spec/protocol/quickslot-bootstrap.md`
- `docs/qa/manual-client-checklist.md`

Next slices:

1. ~~Harden the bootstrap `/open_safebox` fail-closed edge for out-of-range size (`/open_safebox 4`) so it emits no frames, does not open the busy flag, and does not fall through as ordinary chat.~~ Done: recognized out-of-range / invalid `/open_safebox` attempts are consumed fail-closed with no `SAFEBOX_SIZE`, no talking-chat fallthrough, and no same-socket open/busy mutation.
2. ~~Implement the contract-frozen confirm-after-preview refine success path for `probability = 100` only (preview opens same-socket dialog; matching confirm consumes gold/materials, replaces source `vnum`, persists, emits self-only refreshes; `type = 255` cancels; busy windows / lower probability stay fail-closed).~~ Done: preview remembers a same-socket dialog; matching `probability = 100` confirm consumes gold/materials, replaces source `vnum` in-place, persists, and emits self-only material refreshes + material-removal `GC::QUICKSLOT_DEL` for fully consumed material cells + result `ITEM_SET` + gold `PLAYER_POINT_CHANGE` + `CHAT_TYPE_COMMAND` `RefineSuceeded <type>`; `type = 255` cancels; busy merchant/exchange/safebox confirm attempts stay fail-closed. ~~Also own deterministic `probability = 0` destroy + `RefineFailed <type>`.~~ Done: matching `probability = 0` confirm consumes gold/materials, destroys the source cell, syncs material/source quickslots, persists, and emits self-only material refreshes + source `ITEM_DEL` + gold `PLAYER_POINT_CHANGE` + `CHAT_TYPE_COMMAND` `RefineFailed <type>`. ~~Next: freeze then implement injected-roll confirm for `probability` in `1..99` (`docs/plans/2026-08-22-refine-confirm-probability-1-99-injected-roll.md`); keep-grade/catalyst variants stay deferred.~~ Done: matching `probability` in `1..99` confirm draws one roll via `takeRefineConfirmRoll()` (`crypto/rand` production; `QueueRefineConfirmRollForTest` for tests), routes `roll <= probability` to the owned success burst / `RefineSuceeded` and `roll > probability` to the owned destroy burst / `RefineFailed` (including destroy-source quickslot sync), and fails closed for out-of-range rolls; template-authored `keep_on_fail` keep-failure is now owned (`docs/plans/2026-08-29-refine-keep-on-fail.md`); catalyst/downgrade variants stay deferred. ~~Next: restart-restored exclusive ground OwnerID rebind on owner rejoin (`docs/plans/2026-08-22-ground-item-exclusive-owner-id-rebind.md`).~~ Done: rematerialized exclusive pending handles rebind process-local `OwnerID` on matching owner `Join` while peers stay fail-closed and the durable snapshot still omits `OwnerID`; ~~Next: graceful Leave→FileStore owned-ground deletion (`docs/plans/2026-08-22-ground-item-leave-persist-owned-deletion.md`).~~ Done: Leave / stale reclaim persist owned-ground deletion through the existing ground-items changed hook; partner player-shop / cube busy rejects stay deferred.
3. ~~Extend remaining item-template restriction/feedback edges only where a client-visible gap remains after the owned class/sex/level/anti-flag/equip-slot guards.~~ Done for the direct `ITEM_USE` / `/use_item` seam and for active-shell `EXCHANGE ITEM_ADD`: authored `use_reject_message` freezes self-only info-chat feedback for transfer guards / selected-character restrictions / quest-applicable guards on direct use, and authored `give_reject_message` now freezes the same transfer/selected-character feedback set for exchange display add; omitted text stays silent/no-frame. Broader remaining gaps (for example real party membership on ground ownership notices) stay deferred; restart-restored absolute ownership/despawn timers are already owned. ~~Next: reclassify `confirm_when_use` as client-local confirm + ordinary `ITEM_USE` (`docs/plans/2026-08-22-item-use-confirm-when-use-ordinary-path.md`); keep `quest_use` / `applicable` fail-closed.~~ Done: `confirm_when_use` follows the ordinary consumable success path and is no longer a `use_reject_message` store guard; `quest_use` / `quest_use_multiple` / `applicable` stay fail-closed. ~~Next: name empire anti-flags in merchant buy/sell restriction matrix (`docs/plans/2026-08-22-merchant-buy-sell-empire-restriction-matrix.md`).~~ Done: protocol/QA/player/session coverage freezes `anti_empire_a` / `anti_empire_b` / `anti_empire_c` beside job/sex/`min_level` for `SHOP BUY` / `SHOP SELL2` through the already-owned `CanUseTemplate` path; durable safebox password/money/persistence and partner player-shop/cube busy rejects stay deferred. ~~Next: freeze durable safebox persistence contract before RED (`docs/plans/2026-08-22-durable-safebox-persistence-contract-freeze.md`); password/money/mall and partner player-shop/cube busy rejects stay deferred.~~ Done for bootstrap scope: dedicated `safeboxstore` FileStore keyed by account login + character id rematerializes check-in/out/move cells across reconnect / process restart / Leave on the next open; password/money/mall and partner player-shop/cube busy rejects stay deferred. ~~Next: occupied wear-slot equip reject chat (`docs/plans/2026-08-22-equip-occupied-wear-slot-reject-chat.md`); swap/replace stays deferred.~~ Done: packet `ITEM_MOVE` / `/equip_item` onto an already-occupied authored wear cell emit self-only `CHAT_TYPE_INFO` `You are already wearing equipment.` (exchange teardown before chat when active); empty-slot equip and template `equip_reject_message` stay unchanged; occupied-wear swap/replace stays deferred. ~~Next: freeze then implement safebox password challenge / `SAFEBOX_WRONG_PASSWORD` (`docs/plans/2026-08-23-safebox-password-challenge.md`); money/mall stay deferred.~~ Done: warehouse `open_safebox` emits `ShowMeSafeboxPassword`, `/safebox_password` opens on match (default `000000` or durable optional password) or emits header-only `SAFEBOX_WRONG_PASSWORD` / malformed info-chat, while slash `/open_safebox` remains the no-password lab harness; money/mall/`safebox_change_password` stay deferred. ~~Next: freeze `/safebox_change_password` contract before RED (`docs/plans/2026-08-23-safebox-change-password-contract-freeze.md`); money/mall stay deferred.~~ Done for docs-first freeze. ~~Next: implement `/safebox_change_password` durable mutation.~~ Done: slash `/safebox_change_password <old> <new>` persists durable password with success chat `The warehouse password has been changed.` / wrong-password info chat, without opening presentation or inventing `SAFEBOX_CHANGE_PASSWORD` packets; money/mall stay deferred. ~~Next: freeze safebox money / `SAFEBOX_MONEY_CHANGE` before RED (`docs/plans/2026-08-23-safebox-money-change-contract-freeze.md`); mall and client `SAFEBOX_CHANGE_PASSWORD` packets stay deferred.~~ Done for docs-first freeze. ~~Next: implement durable warehouse money / open-burst `SAFEBOX_MONEY_CHANGE` + `/safebox_money_save` / `/safebox_money_withdraw`.~~ Done: optional durable `money`, open-burst emission, and slash deposit/withdraw with gold `PLAYER_POINT_CHANGE` + `SAFEBOX_MONEY_CHANGE`; mall and client `SAFEBOX_CHANGE_PASSWORD` / TMP4 CG `SAFEBOX_MONEY` stay deferred. ~~Next: freeze occupied-wear swap/replace before RED (`docs/plans/2026-08-23-equip-occupied-wear-slot-swap-replace-contract-freeze.md`); mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred.~~ Done for docs-first freeze. ~~Next: implement occupied-wear swap/replace on `ITEM_MOVE` / `/equip_item`.~~ Done: occupied wear with a compatible empty source cell swaps worn↔carried, inverts then applies template `equip_effect`, persists inventory/equipment/points, and closes an active exchange shell before refresh; locked/irremovable/non-swappable destinations keep occupied reject chat / unequip reject text; mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred. ~~Next: freeze mall open/checkout or TMP4 CG `SAFEBOX_MONEY` before RED; player-shop/cube busy rejects stay deferred.~~ Done as docs note only: mall stays anti-goal for Track C and TMP4 has no CG `SAFEBOX_MONEY` header beside check-in/out/move. ~~Next: freeze warehouse reopen cooldown / distance-from-open-anchor before RED (`docs/plans/2026-08-23-safebox-reopen-cooldown-distance-contract-freeze.md`); mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred.~~ Done for docs-first freeze. ~~Next: implement warehouse reopen cooldown / open-anchor distance gates on `/safebox_password`.~~ Done: `/safebox_password` rejects within 10s of CloseSafebox and when `ApproxDistance` from the challenge-time open anchor exceeds `1000`, with self-only info chat and pending preserved; lab `/open_safebox` stays exempt; mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred. ~~Next: freeze already-open warehouse walk-away auto-close before RED (`docs/plans/2026-08-23-safebox-open-walkaway-autoclose-contract-freeze.md`); mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred.~~ Done for docs-first freeze. ~~Next: implement already-open warehouse walk-away auto-close.~~ Done: already-open MOVE / SyncPosition beyond the remembered open anchor queues self-only `CloseSafebox`, clears pending if present, and arms the owned 10s reopen cooldown; pending-only challenges stay on the password distance reject path; mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred. ~~Next: freeze then implement safebox money busy-shell auto-close (`docs/plans/2026-08-23-safebox-money-busy-shell-auto-close.md`); mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred.~~ Done: successful `/safebox_money_save` / `/safebox_money_withdraw` prepend `GC::SHOP END` and/or self/peer `GC::EXCHANGE END` (SHOP before exchange) before gold/`SAFEBOX_MONEY_CHANGE`; fail-closed money rejects leave merchant/exchange shells untouched; mall / TMP4 CG `SAFEBOX_MONEY` / player-shop/cube busy rejects stay deferred. ~~Next: freeze then implement the `CG::MYSHOP` (`0x0802`) codec-only seam (`docs/plans/2026-08-23-myshop-codec-contract-freeze.md`); private-shop runtime open/close/`GC::SHOP_SIGN`/exchange busy rejects stay deferred.~~ Done: `internal/proto/shop` owns `HeaderClientMyShop` / encode/decode with fail-closed count/length guards; GAME dispatch still treats `0x0802` as unexpected; private-shop runtime open/close/`GC::SHOP_SIGN`/exchange busy rejects stay deferred. ~~Next: freeze then implement GAME deny-no-response `MYSHOP` dispatch (decode + default `Accepted:false`, no frames / no disconnect) before any accepted private-shop open path (`docs/plans/2026-08-23-myshop-deny-no-response-dispatch-contract-freeze.md`).~~ Done: GAME routes owned `CG::MYSHOP` through `HandleMyShop` with default deny-no-response; private-shop runtime open/close/`GC::SHOP_SIGN`/exchange busy rejects stay deferred. ~~Next: freeze then implement `GC::SHOP_SIGN` (`0x0811`) codec-only seam before any accepted private-shop open path (`docs/plans/2026-08-23-shop-sign-codec-contract-freeze.md`).~~ Done: `internal/proto/shop` owns `HeaderServerShopSign` / encode/decode with `vid` + `sign[33]`, empty clear/close sign, and fail-closed decode; GAME emission and accepted private-shop open stay deferred. ~~Next: freeze the first accepted private-shop open presentation contract (host-only stock/sign validation + `GC::SHOP_SIGN` emission) before RED; guest browse/buy and partner player-shop/cube exchange busy rejects stay deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-23-myshop-accepted-open-presentation-contract-freeze.md`). ~~Next: implement the frozen host-only accepted private-shop open presentation (`GC::SHOP_SIGN` emission + open/busy flag); guest browse/buy and partner player-shop/cube exchange busy rejects stay deferred.~~ Done: host-only accepted `CG::MYSHOP` open remembers a same-socket busy flag and emits one live `GC::SHOP_SIGN` after sign/stock/busy-shell gates without mutating inventory/gold; empty-sign close / guest browse/buy and partner player-shop/cube exchange busy rejects stay deferred. ~~Next: freeze then implement host-only empty-sign `GC::SHOP_SIGN` clear/close companion (`docs/plans/2026-08-23-myshop-close-sign-clear-contract-freeze.md`); guest browse/buy and partner player-shop/cube exchange busy rejects stay deferred.~~ Done for docs-first freeze. ~~Next: implement host-only empty-sign `GC::SHOP_SIGN` clear/close companion on lifecycle teardown; guest browse/buy and partner player-shop/cube exchange busy rejects stay deferred.~~ Done: lifecycle `/phase_select` / `/quit` / `/logout`, practice-mob floor, transfer/warp, and lab `/close_myshop` clear the same-socket busy flag with one empty-sign `GC::SHOP_SIGN` (merchant `SHOP END` before empty sign before exchange `END` when those shells close together); guest browse/buy and partner player-shop/cube exchange busy rejects stay deferred. ~~Next: freeze then implement partner-side open player-shop exchange START/ACCEPT busy rejects once guest browse or peer sign around-broadcast exists; cube busy rejects stay deferred.~~ Done for docs-first freeze without requiring guest browse (`docs/plans/2026-08-24-exchange-myshop-busy-window-reject-chat.md`): publish a peer-visible open-private-shop busy bit and reuse merchant/safebox/refine busy chat strings. ~~Next: implement open-private-shop exchange START/ACCEPT/commit busy rejects; cube busy rejects stay deferred.~~ Done: peer-visible MyShop busy bit + requester/partner busy info-chat on START/ACCEPT/commit. ~~Next: freeze then implement open-MYSHOP host item-mutation fail-closed lock (`docs/plans/2026-08-24-myshop-open-host-item-mutation-lock.md`); guest browse/buy and cube busy rejects stay deferred.~~ Done: open MYSHOP fails closed for host item use/move/drop/pickup/give/safebox/refine mutations with no frames. ~~Next: freeze peer `SHOP_SIGN` around-broadcast before RED (`docs/plans/2026-08-24-myshop-peer-shop-sign-around-broadcast-contract-freeze.md`); guest browse/buy and cube busy rejects stay deferred.~~ Done for docs-first freeze. ~~Next: implement peer `SHOP_SIGN` around-broadcast.~~ Done for open/close peer fanout of live/empty `GC::SHOP_SIGN`. ~~Next: freeze then implement view-entry rematerialization of a remembered live `SHOP_SIGN` when a peer newly becomes visible to an already-open host (`docs/plans/2026-08-24-myshop-peer-shop-sign-view-entry-rematerialization-contract-freeze.md`); guest browse/buy and cube busy rejects stay deferred.~~ Done for docs-first freeze. ~~Next: implement view-entry rematerialization.~~ Done: newly visible peers receive one rematerialized live `GC::SHOP_SIGN` for an already-open host after ordinary peer visibility bootstrap; guest browse/buy and cube busy rejects stay deferred. ~~Next: freeze then implement the first guest browse / open-presentation seam for an already-visible private shop; cube busy rejects stay deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-24-myshop-guest-browse-open-presentation-contract-freeze.md`): visible open host → peer `ON_CLICK` → busy gates → one `GC::SHOP START` stock table; buy/sell / guest leave/`END` / cube busy rejects stay deferred. ~~Next: implement the frozen guest browse open presentation.~~ Done: guest `ON_CLICK` against a visible already-open MYSHOP host emits one guest-only `GC::SHOP START` from remembered stock + live matching carried cell sockets/attrs; busy guest/host shells reuse owned exchange busy info-chat; guest own open MYSHOP / closed host stay silent; buy/sell / guest leave/`END` / cube busy rejects stay deferred. ~~Next: freeze then implement guest browse leave/`SHOP END` (RemoveGuest) before buy/sell mutation; cube busy rejects stay deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-24-myshop-guest-browse-leave-end-contract-freeze.md`): guest `CG::SHOP END` / host empty-sign / guest lifecycle clear the remembered browse flag with one guest-only `GC::SHOP END`; buy/sell and cube busy rejects stay deferred. ~~Next: implement the frozen guest browse leave/`SHOP END` presentation.~~ Done: guest `CG::SHOP END` / host empty-sign / guest lifecycle clear browse with one guest-only `GC::SHOP END`; buy/sell and cube busy rejects stay deferred. ~~Next: freeze then implement the first guest private-shop buy mutation against an already-open MYSHOP; cube busy rejects stay deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-24-myshop-guest-buy-mutation-contract-freeze.md`): guest `CG::SHOP BUY` while browsing transfers live host stock, debits/credits gold, clears the sold display slot with guest `UPDATE_ITEM(vnum=0)`, and keeps distance / sold-out / inventory-full / insufficient-gold fail-closed; tax/empire multipliers, guest sell-into-PC-shop, and cube busy rejects stay deferred. ~~Next: implement the frozen guest private-shop buy mutation; cube busy rejects stay deferred.~~ Done: guest `CG::SHOP BUY` while browsing transfers live host stock, debits/credits gold, persists both sides, emits guest/host inventory+gold refreshes plus buyer `UPDATE_ITEM(vnum=0)`, and fail-closes distance / sold-out / inventory-full / insufficient-gold / host gold-carrier overflow; tax/empire multipliers, guest sell-into-PC-shop, and cube busy rejects stay deferred. ~~Next: prove multi-guest sold-slot `UPDATE_ITEM` fan-out for a still-browsing watcher (`docs/plans/2026-08-24-myshop-guest-buy-multi-guest-update-item-fanout.md`); cube busy rejects stay deferred.~~ Done: second browsing guest receives queued `UPDATE_ITEM(vnum=0)` with no watcher inventory/gold mutation and sold-out retry on the cleared slot; tax/empire multipliers, guest sell-into-PC-shop, shop-bag consumption, and cube busy rejects stay deferred. ~~Next: freeze then implement cube open/close presentation + peer-visible busy bit before busy rejects (`docs/plans/2026-08-25-cube-open-close-presentation-busy-bit.md`); guest sell-into-PC-shop stays deferred.~~ Done: lab `/open_cube` / `/close_cube` emit `CHAT_TYPE_COMMAND` `cube open <npcVnum>` / `cube close`, publish a peer-visible cube busy bit, reject already-open / busy merchant|safebox|refine|MYSHOP|exchange shells with the oracle EN strings, and clear on lifecycle/floor/transfer; recipe make/add/list stay deferred. ~~Next: freeze then implement cube busy rejects for exchange / MYSHOP / safebox / refine presentation shells; guest sell-into-PC-shop stays deferred.~~ Done for exchange: open cube rejects START/ACCEPT/commit with the already-owned merchant/safebox/refine/MYSHOP busy chat strings (`docs/plans/2026-08-25-exchange-cube-busy-window-reject-chat.md`). ~~Next: freeze then implement MYSHOP / safebox / refine open/confirm cube busy rejects; guest sell-into-PC-shop and recipe make/add/list stay deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-25-myshop-safebox-refine-cube-busy-rejects.md`). ~~Next: implement host MYSHOP open / guest browse / safebox open / refine confirm cube busy rejects; guest sell-into-PC-shop and recipe make/add/list stay deferred.~~ Done for host MYSHOP open: open cube rejects `CG::MYSHOP` with requester busy info-chat and no `SHOP_SIGN`. ~~Next: implement guest browse / safebox open / refine confirm cube busy rejects; guest sell-into-PC-shop and recipe make/add/list stay deferred.~~ Done: guest browse / safebox open / refine confirm cube busy rejects are owned beside host MYSHOP open (`docs/plans/2026-08-25-myshop-safebox-refine-cube-busy-rejects.md`). ~~Next: prove guest `SHOP SELL` / `SELL2` while browsing stay fail-closed (`docs/plans/2026-08-25-myshop-guest-sell-while-browsing-fail-closed.md`); recipe make/add/list stay deferred.~~ Done: session proof + docs/spec/QA own silent/no-mutation guest sell-into-PC-shop rejects. ~~Next: freeze cube `r_info` → `r_list` before RED (`docs/plans/2026-08-25-cube-r-info-result-list-contract-freeze.md`); `m_info` / add / delete / make stay deferred.~~ Done for docs-first freeze. ~~Next: implement remembered `activeCubeNPCVnum` + `/cube r_info` → `cube r_list`; `m_info` / add / delete / make stay deferred.~~ Done: `/open_cube` remembers `activeCubeNPCVnum`, `/cube r_info` emits authored `cube r_list` from `internal/cubestore` bootstrap NPC `20022`, and missing/empty/oversize/extra-arg paths stay silent (`docs/plans/2026-08-25-cube-r-info-result-list-implementation.md`). ~~Next: freeze then implement `/cube r_info <index> [count]` → `cube m_info` before add/delete/make mutation; keep craft `make` deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-25-cube-m-info-material-info-contract-freeze.md`). ~~Next: implement `/cube r_info <index> [count]` → `cube m_info` from cubestore materials/gold; add/delete/make stay deferred.~~ Done: `/cube r_info <index>` / `/cube r_info <index> <count>` emit authored `cube m_info` from cubestore materials/gold with silent fail-closed for closed/past-end/malformed/oversize (`docs/plans/2026-08-25-cube-m-info-material-info-implementation.md`). ~~Next: freeze then implement the first craft-slot mutation (`cube add` / `delete` / `list`) before inventing `make`; keep complicated OR-material text deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-25-cube-add-del-slot-binding-contract-freeze.md`): `/cube add` / `/cube del` bind inventory cells and emit `cube info <gold> 0 0` without inventory mutation; `list` / `cancel` / `make` stay deferred. ~~Next: implement `/cube add` / `/cube del` → `cube info` craft-slot binding; keep `make` deferred.~~ Done: `/cube add` / `/cube del|delete` remember same-socket inventory-cell pointers and emit authored `cube info <gold> 0 0` without inventory/gold mutation (`docs/plans/2026-08-25-cube-add-del-slot-binding-implementation.md`). ~~Next: freeze then implement `cube make` (or `cube list` INFO dump) from bound slots + authored recipes; keep `cancel` / complicated OR-materials deferred.~~ Done for docs-first freeze (`docs/plans/2026-08-25-cube-make-percent-100-contract-freeze.md`): deterministic `/cube make` with authored `percent = 100` consumes bound materials/gold and emits `cube success <vnum> <count>`; `make all` / fail rolls / `list` / `cancel` stay deferred. ~~Next: implement `/cube make` (`percent = 100`) from bound slots + authored recipes; keep `make all` / fail rolls deferred.~~ Done: `/cube make` consumes bound materials/gold for authored `percent = 100`, grants the reward, persists inventory/gold/quickslots, and emits material refreshes + emptied-cell `QUICKSLOT_DEL` + reward SET/UPDATE + gold `PLAYER_POINT_CHANGE` + `cube success` + follow-up `cube info` (`docs/plans/2026-08-25-cube-make-percent-100-implementation.md`). ~~Next: freeze then implement cube fail-roll `percent` in `0..99` or `make all` / `list` / `cancel`; keep complicated OR-materials deferred.~~ Done for injected-roll `percent` in `1..99` (`docs/plans/2026-08-26-cube-make-percent-1-99-injected-roll.md`). ~~Next: freeze then implement `/cube make all` loop; keep `list` / `cancel` / store `percent = 0` deferred.~~ Done: `/cube make all` loops the owned one-attempt make path while each attempt succeeds and stops on the first fail-roll or pre-mutation reject (`docs/plans/2026-08-26-cube-make-all-loop.md`). ~~Next: freeze then implement `cube list` / `cancel` or store-level `percent = 0` always-fail; keep complicated OR-materials deferred.~~ Done for docs-first freeze of store-level `percent = 0` always-fail (`docs/plans/2026-08-26-cube-make-percent-0-always-fail.md`). ~~Next: implement store-accepted `percent = 0` always-fail make path; keep `cube list` / `cancel` deferred.~~ Done: `cubestore` accepts `percent` in `0..100` (explicit `0` persists; omitted loads as `0`), and `/cube make` / `/cube make all` always-fail consume without drawing a roll (`docs/plans/2026-08-26-cube-make-percent-0-always-fail.md`). ~~Next: freeze then implement `cube list` / `cancel`; keep complicated OR-materials deferred.~~ Done: `/cube list` emits ordered self-only INFO rows `cube[<i>]: inventory[<cell>]: <template.name>` for live bound slots, and `/cube cancel` / `/cube close` alias the owned `/close_cube` presentation close (`docs/plans/2026-08-26-cube-list-cancel.md`). ~~Next: prefer the next Track C economy/storage hardening gap over inventing OR-materials.~~ Done for MYSHOP open reject-chat hardening: worn body armor / `anti_give|anti_myshop` / locked stock / gold-overflow emit self-only INFO chat instead of silent fail (`docs/plans/2026-08-26-myshop-open-reject-chat-hardening.md`). ~~Next: ordinary shop-bag `50200` require-and-consume on accepted open (`docs/plans/2026-08-27-myshop-shop-bag-50200-require-consume.md`).~~ Done for bootstrap scope: after armor/cash/locked/gold gates, accepted open consumes one unlisted carried `50200`, persists, emits bag refresh then `SHOP_SIGN`; missing/listed-only bag stays silent (`docs/plans/2026-08-27-myshop-shop-bag-50200-require-consume.md`). ~~Next: MYSHOP open equipped-stock INFO reject chat (`docs/plans/2026-08-27-myshop-open-equipped-stock-reject-chat.md`).~~ Done for the deferred exchange-shell `/restart_town` destination-peer recovery twin beside the already-owned `/restart_here` proofs (`docs/plans/2026-08-26-exchange-practice-mob-floor-restart-town-exchange-recovery.md`). ~~Next: MYSHOP open equipped-stock INFO reject chat (`docs/plans/2026-08-27-myshop-open-equipped-stock-reject-chat.md`).~~ Done: listed equipped / equipment-window stock emits self-only `Equipped items cannot be sold in a private shop.` after cash-item and before locked, with no bag debit / no `SHOP_SIGN` (`docs/plans/2026-08-27-myshop-open-equipped-stock-reject-chat.md`). ~~Next: prefer the next Track C economy/storage hardening gap (silk-bag `71049` / pricelist, banword/quest-running open blocks, or complicated OR-materials / quest-NPC cube open / binary cube headers) only when client-visible evidence requires them.~~ Done for docs-first freeze of silk-bag `71049` consume-skip without inventing `MYSHOP_PRICELIST` (`docs/plans/2026-08-27-myshop-silk-bag-71049-consume-skip.md`). ~~Next: implement silk-bag `71049` consume-skip on accepted MYSHOP open; keep `MYSHOP_PRICELIST` / banword / quest-running / OR-materials deferred.~~ Done for bootstrap scope: unlisted carried `71049` opens with bag-less `SHOP_SIGN` and skips ordinary `50200` consume; listed-only / locked-only silk fall through to ordinary bag or silent miss (`docs/plans/2026-08-27-myshop-silk-bag-71049-consume-skip.md`). ~~Next: prefer the next Track C economy/storage hardening gap (`MYSHOP_PRICELIST` DB rematerialize, banword/quest-running open blocks, or complicated OR-materials / quest-NPC cube open / binary cube headers) only when client-visible evidence requires them.~~ Done for docs-first freeze of MYSHOP open banword sign reject (`docs/plans/2026-08-27-myshop-open-banword-sign-reject.md`): bootstrap substring list + EN INFO `You can't give your shop an invalid name.` after armor and before stock walk; Canada skip / DB reload / quest-running / `MYSHOP_PRICELIST` stay deferred. ~~Next: implement MYSHOP open banword sign reject; keep Canada skip / DB banword reload / quest-running / `MYSHOP_PRICELIST` deferred.~~ Done for bootstrap scope: non-empty signs matching the bootstrap banword list reject with self-only INFO `You can't give your shop an invalid name.` after armor and before stock walk, with no bag debit / no `SHOP_SIGN` (`docs/plans/2026-08-27-myshop-open-banword-sign-reject.md`). ~~Next: prefer the next Track C economy/storage hardening gap (`MYSHOP_PRICELIST` DB rematerialize, quest-running open blocks, or complicated OR-materials / quest-NPC cube open / binary cube headers) only when client-visible evidence requires them.~~ Done for docs-first freeze of authored `myshop_reject_message` on MYSHOP open cash-item stock (`docs/plans/2026-08-27-myshop-open-myshop-reject-message.md`): optional template text overrides fixed English when `anti_myshop|anti_give` guards are present; quest-running / `MYSHOP_PRICELIST` / OR-materials stay deferred. ~~Next: implement authored `myshop_reject_message` store + MYSHOP open cash-item override; keep quest-running / `MYSHOP_PRICELIST` / OR-materials deferred.~~ Done for bootstrap scope: optional `myshop_reject_message` round-trips through itemstore with fail-closed unguarded/NUL rejects, and MYSHOP open cash-item stock emits the authored INFO override when present (`docs/plans/2026-08-27-myshop-open-myshop-reject-message.md`). ~~Next: prefer the next Track C economy/storage hardening gap (`MYSHOP_PRICELIST` DB rematerialize, quest-running open blocks, or complicated OR-materials / quest-NPC cube open / binary cube headers) only when client-visible evidence requires them.~~ Done for docs-first freeze of MYSHOP open gold-overflow gate reorder (`docs/plans/2026-08-27-myshop-open-gold-overflow-gate-reorder.md`): gold INFO after armor and before banword/stock walk; quest-running / `MYSHOP_PRICELIST` / OR-materials stay deferred. ~~Next: implement MYSHOP open gold-overflow gate reorder; keep quest-running / `MYSHOP_PRICELIST` / OR-materials deferred.~~ Done for bootstrap scope: gold INFO after armor and before banword/stock walk (`docs/plans/2026-08-27-myshop-open-gold-overflow-gate-reorder.md`). ~~Next: prefer the next Track C economy/storage hardening gap (`MYSHOP_PRICELIST` DB rematerialize, quest-running open blocks, or complicated OR-materials / quest-NPC cube open / binary cube headers) only when client-visible evidence requires them.~~ Done for docs-first freeze of MYSHOP already-open second open → empty-sign close (`docs/plans/2026-08-27-myshop-open-already-open-second-open-empty-sign-close.md`): second accepted decode clears with owned empty-sign companion instead of busy INFO; quest-running / `MYSHOP_PRICELIST` / OR-materials stay deferred. ~~Next: implement MYSHOP already-open second open → empty-sign close; keep quest-running / `MYSHOP_PRICELIST` / OR-materials deferred.~~ Done: already-open second accepted-decode `CG::MYSHOP` clears with the owned empty-sign companion (self + peer around-broadcast, guest browse END as already owned, no busy INFO, no bag refund) after other busy shells and body-armor, before gold/banword/stock; empty-sign/zero-count second packets stay silent while open (`docs/plans/2026-08-27-myshop-open-already-open-second-open-empty-sign-close.md`). ~~Next: freeze then implement the next evidence-backed MYSHOP open/close gap (`MYSHOP_PRICELIST` DB rematerialize, quest-running open blocks, or bag-missing INFO) only when client-visible evidence requires them; keep OR-materials / tax/empire multipliers deferred.~~ Done for accepted MYSHOP open cancel-active-exchange (`docs/plans/2026-08-28-myshop-open-cancel-active-exchange.md`): success cancels an open exchange before live `SHOP_SIGN` (bag and silk paths); reject/busy paths leave the shell open; merchant/safebox/refine/cube busy rejects stay unchanged. ~~Next: freeze then implement the next evidence-backed MYSHOP open/close gap (`MYSHOP_PRICELIST` DB rematerialize, quest-running open blocks, or bag-missing INFO) only when client-visible evidence requires them; keep OR-materials / tax/empire multipliers deferred.~~ Done for docs/spec freeze of MYSHOP open auto-potion `socket0` deactivate + minimal instance-sockets substrate (`docs/plans/2026-08-28-myshop-open-auto-potion-socket0-deactivate.md`); GREEN is the next RED. `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / shopkeeper polymorph / tax/empire stay deferred. ~~Next: implement the frozen MYSHOP open auto-potion `socket0` deactivate + instance-sockets substrate (`docs/plans/2026-08-28-myshop-open-auto-potion-socket0-deactivate.md`); keep `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / shopkeeper polymorph / refine keep-grade deferred.~~ Done for bootstrap scope: minimal per-instance inventory sockets round-trip through account FileStore and prefer instance presence (including explicit `0`) over template fallback on carried `ITEM_SET` / `ITEM_UPDATE` / guest browse stock encode; accepted MYSHOP open success clears listed auto-potion `socket0` (`72723..72730` when effective `socket0 == 1`) with self-only `ITEM_UPDATE` after bag refresh / before exchange `END` / live `SHOP_SIGN` on bag and silk paths, and reject paths stay non-mutating (`docs/plans/2026-08-28-myshop-open-auto-potion-socket0-deactivate.md`). ~~Next: freeze then implement the next evidence-backed EXCHANGE / MYSHOP / storage gap (`LESS_GOLD` auto-cancel stays out; prefer `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / refine keep-grade only with client-visible evidence).~~
4. Keep partner-side open player-shop / cube busy-window rejection text deferred until those presentation seams exist. Open refine-dialog exchange `START` busy rejects are now owned beside merchant/safebox; exchange `ACCEPT` / second-accept mutual-accept finalize now also fail closed when either paired side has an open merchant/safebox/refine busy presentation and emit the same self-only requester/partner busy info-chat strings already owned by `START`; exact-position transfer / warp rebootstrap now tears down an open exchange shell with self/peer `GC::EXCHANGE END` before the transfer burst (including same-map in-range destinations); mutual-accept `CommitExchangeFinalize` now revalidates busy windows plus displayed item/gold / receiver finalization preconditions at commit time so post-plan drift cannot sneak past the accept-time gate; and commit-time busy-window drift now returns those same self-only START/ACCEPT busy info-chat strings to the commit requester; second-accept / commit-time Check-shaped displayed item/gold drift and inventory-capacity failures now emit dual-sided info-chat (`Not enough Yang or the item is not in place.` / partner wording; `There isn't enough space in your inventory.` / partner wording) while other non-busy receiver precondition failures stay silent/no-frame (`docs/plans/2026-08-22-exchange-finalize-check-space-reject-chat.md`). ~~Next: freeze then implement dual-sided gold-overflow finalize reject chat (`docs/plans/2026-08-22-exchange-finalize-gold-overflow-reject-chat.md`); id-collision / restriction rejects stay silent.~~ Done: second-accept / commit-time receiver gold-overflow emits dual-sided `You cannot carry any more gold.` / `The other person cannot carry any more gold.`; id-collision / restriction rejects stay silent. ~~Next: freeze then implement dual-sided mutual-accept success chat (`docs/plans/2026-08-22-exchange-finalize-success-chat.md`); id-collision / restriction rejects stay silent.~~ Done: mutual-accept finalize emits dual-sided `The trade with <partner_name> has been successful.` before shell `END`; id-collision / restriction rejects stay silent. ~~Next: freeze then implement `EXCHANGE START` gold-carrier-cap reject chat (`docs/plans/2026-08-22-exchange-start-gold-carrier-cap-reject-chat.md`); id-collision / restriction rejects stay silent.~~ Done: `EXCHANGE START` emits self-only gold-carrier-cap reject chat when either side already holds `>= 1<<31-1` gold (requester `You have more than 2 Billion Yang. You cannot trade.` / partner `The player has more than 2 Billion Yang. You cannot trade with him.`, local-first requester-wins after busy/`ALREADY` gates). ~~Next: freeze then implement `EXCHANGE ACCEPT` / commit-time gold-carrier-cap reject chat (`docs/plans/2026-08-22-exchange-accept-gold-carrier-cap-reject-chat.md`); id-collision / restriction rejects stay silent.~~ Done: `EXCHANGE ACCEPT` / commit-time emit the same self-only gold-carrier-cap reject chat when either paired side drifts to `>= 1<<31-1` after shell open (after busy gates; local-first requester-wins). ~~Next: freeze dual-sided exchange finalize Other reject chat (`Unknown error`) for id-collision / restriction / over-max / locked-compatible-stack (`docs/plans/2026-08-22-exchange-finalize-other-reject-chat.md`); player-shop/cube busy rejects stay deferred.~~ Done: second-accept / commit-time Other emit dual-sided `Unknown error`; player-shop/cube busy rejects stay deferred. ~~Also: freeze then implement the first accepted in-memory `SAFEBOX_CHECKIN` while `/open_safebox` is already open (`docs/plans/2026-08-21-safebox-checkin-in-memory-mutation.md`); password/load/checkout/money/durable persistence stay deferred.~~ Done for bootstrap scope: accepted open-presentation `SAFEBOX_CHECKIN` removes a carried stack, syncs source item quickslots, stores same-session in-memory safebox contents, emits `ITEM_DEL` + `SAFEBOX_SET`, re-emits remembered rows on reopen, and closes an active exchange shell on success. ~~Next: freeze then implement the first accepted in-memory `SAFEBOX_CHECKOUT` while `/open_safebox` is already open (`docs/plans/2026-08-21-safebox-checkout-in-memory-mutation.md`); password/load/move/money/durable persistence stay deferred.~~ Done for bootstrap scope: accepted open-presentation `SAFEBOX_CHECKOUT` removes a remembered same-session safebox cell, places/merges into carried inventory with `SAFEBOX_DEL` + `ITEM_SET` / `ITEM_UPDATE`, persists inventory, and closes an active exchange shell on success. ~~Next: freeze then implement the first accepted same-session `SAFEBOX_ITEM_MOVE` while `/open_safebox` is already open (`docs/plans/2026-08-21-safebox-item-move-in-memory-mutation.md`); password/load/money/durable persistence stay deferred.~~ ~~Done for bootstrap scope: accepted open-presentation whole-stack `SAFEBOX_ITEM_MOVE` relocates into an empty safebox cell or merges into a compatible same-`vnum` cell under template `max_count`, emits self-only `SAFEBOX_DEL` + `SAFEBOX_SET` without inventory/quickslot/gold/account mutation, and closes an active exchange shell on success; partial splits / password/load/money/durable persistence stay deferred.~~ ~~Next: freeze then implement merchant-window auto-close on accepted check-in/out/move success (`docs/plans/2026-08-21-safebox-accepted-mutation-merchant-auto-close.md`).~~ Done for bootstrap scope: accepted open-presentation `SAFEBOX_CHECKIN` / `SAFEBOX_CHECKOUT` / `SAFEBOX_ITEM_MOVE` now emit self-only `GC::SHOP END` before refresh frames when a merchant window is open (SHOP before exchange when both shells are active). ~~Next: freeze then implement partial-count `SAFEBOX_ITEM_MOVE` split / compatible partial merge (`docs/plans/2026-08-21-safebox-item-move-partial-split.md`).~~ Done for bootstrap scope: partial-count empty-destination split allocates a new safebox item identity and emits dual `SAFEBOX_SET`; compatible partial merge refreshes both cells the same way; money / password / durable persistence stay deferred. ~~Next: emit legacy `CHAT_TYPE_COMMAND` `CloseSafebox` on `/close_safebox` / `/safebox_close` (`docs/plans/2026-08-21-safebox-close-command-chat.md`).~~ Done for bootstrap scope: open presentation close returns self-only `CloseSafebox` command chat so TMP4 clients hide the window; already-closed close stays silent/consume; password/load/money/durable persistence stay deferred. Done for bootstrap scope: practice-mob floor, exact-position transfer/warp rebootstrap, and `/phase_select` / `/quit` / `/logout` now also emit self-only `CloseSafebox` when the presentation was open (`docs/plans/2026-08-21-safebox-lifecycle-close-command-chat.md`). Partner-side open player-shop / cube busy-window exchange rejects stay deferred until those presentation seams exist. ~~Next: accept the TMP4 inventory-window `SAFEBOX_ITEM_MOVE` wire (`docs/plans/2026-08-21-safebox-item-move-tmp4-inventory-window-wire.md`).~~ Done for bootstrap scope: open-presentation `SAFEBOX_ITEM_MOVE` now accepts TMP4 `WindowInventory` packed positions (and mixed inventory/safebox tooling) as same-session safebox slot indices beside explicit `WindowSafebox`; mall/equipment/other windows stay fail-closed; money / password / durable persistence stay deferred. ~~Next: freeze docs so `quickslot-bootstrap.md` / packet-matrix own the already-shipped open-presentation `SAFEBOX_CHECKIN` source-cell `QUICKSLOT_DEL` path (`docs/plans/2026-08-21-safebox-checkin-quickslot-sync-docs.md`).~~ Done for bootstrap scope: docs now name accepted check-in quickslot removal sync beside use/sell/drop/refine/exchange; check-out and safebox item-move stay non-quickslot-mutating for carried bindings; mall/timeout/destruction/belt sync and password/money/durable persistence stay deferred. ~~Next: emit legacy `CHAT_TYPE_COMMAND` `RefineSuceeded <type>` after accepted `probability = 100` refine confirm (`docs/plans/2026-08-21-refine-confirm-success-command-chat.md`).~~ Done for bootstrap scope: success confirm burst ends with self-only `RefineSuceeded <type>` after gold `PLAYER_POINT_CHANGE`; `RefineFailed` / lower-probability destroy stay deferred. ~~Next: freeze then implement mutual-accept finalize reject auto-cancel (`docs/plans/2026-08-28-exchange-finalize-reject-auto-cancel.md`); busy / gold-carrier / `LESS_GOLD` / persist-fail stay cancellable.~~ Done: second-accept / commit-time Check/Space/gold-overflow/Other reject chat now auto-cancels with self/peer `GC::EXCHANGE END`. ~~Next: freeze then implement busy-window / gold-carrier-cap reject auto-cancel (`docs/plans/2026-08-28-exchange-busy-gold-carrier-reject-auto-cancel.md`); `LESS_GOLD` / persist-fail stay cancellable.~~ Done: ACCEPT / commit-time busy-window and gold-carrier-cap reject chat now auto-cancels with self/peer `GC::EXCHANGE END`. ~~Next: freeze then implement mutual-accept persist-fail reject chat + auto-cancel (`docs/plans/2026-08-28-exchange-persist-fail-reject-auto-cancel.md`); `LESS_GOLD` auto-cancel stays out.~~ Done: origin/partner account Save failures during mutual-accept finalize emit dual-sided `Unknown error` then self/peer `GC::EXCHANGE END` and clear the shell. ~~Next: freeze then implement the next evidence-backed EXCHANGE / MYSHOP / storage gap (`LESS_GOLD` auto-cancel stays out; prefer `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / refine keep-grade only with client-visible evidence).~~ Done for docs/spec freeze of MYSHOP open auto-potion `socket0` deactivate + minimal instance-sockets substrate (`docs/plans/2026-08-28-myshop-open-auto-potion-socket0-deactivate.md`); GREEN implementation is the next RED. `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / refine keep-grade / shopkeeper polymorph stay deferred; `LESS_GOLD` auto-cancel stays out. ~~Next: implement the frozen MYSHOP open auto-potion `socket0` deactivate + instance-sockets substrate (`docs/plans/2026-08-28-myshop-open-auto-potion-socket0-deactivate.md`); keep `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / shopkeeper polymorph / refine keep-grade deferred.~~ Done for bootstrap scope: minimal per-instance inventory sockets round-trip through account FileStore and prefer instance presence (including explicit `0`) over template fallback on carried `ITEM_SET` / `ITEM_UPDATE` / guest browse stock encode; accepted MYSHOP open success clears listed auto-potion `socket0` (`72723..72730` when effective `socket0 == 1`) with self-only `ITEM_UPDATE` after bag refresh / before exchange `END` / live `SHOP_SIGN` on bag and silk paths, and reject paths stay non-mutating (`docs/plans/2026-08-28-myshop-open-auto-potion-socket0-deactivate.md`). ~~Next: freeze then implement the next evidence-backed EXCHANGE / MYSHOP / storage gap (`LESS_GOLD` auto-cancel stays out; prefer `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / refine keep-grade only with client-visible evidence).~~ Done for bootstrap scope: silk/shop-bag `ITEM_USE` / `/use_item` opens private-shop setup via `MyShopPriceList` + `OpenPrivateShop` command chat with process-local remembered unit prices from silk-path `CG::MYSHOP` open (`docs/plans/2026-08-28-myshop-bag-use-openprivateshop-pricelist.md`). ~~Next: freeze then implement accept-time `LESS_GOLD` auto-cancel (`docs/plans/2026-08-29-exchange-less-gold-accept-auto-cancel.md`); keep over-budget `ELK_ADD` `LESS_GOLD` shell-open, GD/DB `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / refine keep-grade deferred.~~ Done for docs/spec freeze of accept-time `LESS_GOLD` then self/peer `END` (`docs/plans/2026-08-29-exchange-less-gold-accept-auto-cancel.md`). ~~Next: implement accept-time `LESS_GOLD` auto-cancel (`docs/plans/2026-08-29-exchange-less-gold-accept-auto-cancel.md`); keep over-budget `ELK_ADD` `LESS_GOLD` shell-open.~~ Done for bootstrap scope: accept-time requester `LESS_GOLD` emits self `LESS_GOLD` then self/peer `GC::EXCHANGE END` and clears the shell; over-budget gold-add `LESS_GOLD` stays shell-open (`docs/plans/2026-08-29-exchange-less-gold-accept-auto-cancel.md`). ~~Next: freeze then implement the next evidence-backed EXCHANGE / MYSHOP / storage gap (prefer GD/DB `MYSHOP_PRICELIST` durable cache / quest-running / bag-missing INFO / refine keep-grade only with client-visible evidence).~~ Done for docs/spec freeze of `EXCHANGE ITEM_ADD` preferring instance `EffectiveSockets` (`docs/plans/2026-08-28-exchange-item-add-instance-sockets.md`). ~~Next: implement the frozen `EXCHANGE ITEM_ADD` instance-socket display (`docs/plans/2026-08-28-exchange-item-add-instance-sockets.md`); keep `LESS_GOLD` auto-cancel / GD/DB `MYSHOP_PRICELIST` / quest-running / bag-missing INFO / refine keep-grade deferred.~~ Done for bootstrap scope: active-shell `ITEM_ADD` prefers instance `EffectiveSockets` (including explicit zero) over template sockets, omitted instance sockets keep template sockets, attributes stay template-authored, and display remains non-mutating (`docs/plans/2026-08-28-exchange-item-add-instance-sockets.md`). Owned bootstrap exchange Cancel-on-failure companions (Check/Space/gold-overflow/Other, busy/gold-carrier, persist-fail, accept-time requester `LESS_GOLD`) are complete; over-budget gold-add `LESS_GOLD` stays shell-open. ~~Next: freeze then implement the next evidence-backed EXCHANGE / MYSHOP / storage gap (prefer GD/DB `MYSHOP_PRICELIST` durable cache / quest-running / bag-missing INFO / refine keep-grade only with client-visible evidence; do not invent substrate).~~
5. ~~Land the frozen bootstrap safebox-open presentation seam (`/open_safebox` / `/close_safebox` + `SAFEBOX_SIZE`) and wire exchange START requester/partner busy rejects to that open flag; keep password/load/placement/money deferred.~~ Done for bootstrap scope: `/open_safebox [1..3]` emits self-only `SAFEBOX_SIZE`, `/close_safebox` / `/safebox_close` clear the open flag with self-only `CHAT_TYPE_COMMAND` `CloseSafebox`, and exchange START reuses the merchant busy-window chat strings for requester/partner open-safebox rejects without inventing storage mutation. Open refine-dialog presentation now uses the same requester/partner busy chat strings for exchange `START`.
6. ~~Finish integrating pending exchange anti-give/display-slot guards / mutual-accept finalization / ownership timers~~ Done for bootstrap scope on `lane/items`; do not reopen those as if they were still pending.
7. ~~Freeze the first accepted refine success seam in docs/spec before opening RED.~~ Done: `item-refine-bootstrap.md` now owns confirm-after-preview (`probability = 100`, `type = 255` cancel, busy-window fail-closed confirm).
8. ~~Own the bootstrap in-memory ground-item destroy deadline (`300` seconds from registration) so unclaimed public item/gold handles emit `GC::ITEM_GROUND_DEL` instead of littering forever.~~ Done for bootstrap scope, including kill-reward drops that reuse the same exclusive-ownership timer / blank public `ITEM_OWNERSHIP` release / ordinary collector pickup path as player drops; pending handles now also rematerialize across `gamed` process restart from the dedicated ground-item FileStore with absolute ownership/despawn timers.

Exit criteria:

- PvE rewards can become carried or ground items safely,
- common item uses are visible and persistent,
- trade/storage/refine either work in a narrow tested way or fail closed with documented feedback,
- item mutations update quickslots, peers, and persistence consistently.

Anti-goals:

- no broad player-shop or mall work before carried/ground/trade semantics are stable,
- no guessed item-use families without evidence,
- no item/economy behavior that bypasses template validation.

## Track D — Content, NPC services, quests, regen, and drops

**Objective:** make authored content drive the vertical instead of hardcoded examples.

Primary lane:

- `lane/content` / `go-metin2-content-worker`

Likely areas:

- `internal/contentbundle`
- `internal/staticstore`
- `internal/interactionstore`
- `internal/minimal`
- future quest/content packages
- `spec/protocol/static-actor-*`
- `spec/protocol/npc-*`
- `spec/protocol/content-spawn-groups-bootstrap.md`
- `docs/examples/*`

Next slices:

1. Keep content summaries useful, but stop prioritizing endpoint-only work unless it unblocks QA or migration.
2. ~~Add first quest-state seam: quest flags, NPC dialog state, and one simple persisted trigger/result contract.~~ Done for bootstrap scope: `quest_flag` interact CAS, optional service gates, kill-quest credit + require gates, and turn-in NPC close the persisted flag loop.
3. ~~Add regen/drop table ingestion in a deliberately small authored format.~~ Done for bootstrap scope: one-count `regen_spawns` + fixed `drop_tables` expand before import; `docs/examples/bootstrap-pve-vertical-authoring-bundle.json` now composes that authoring form with the quest NPC loop.
4. Expand NPC service kinds only when client-visible behavior is owned; `open_safebox` is now the third owned service family beside `warp` / `shop_preview`, and gated warehouse mismatch previews are covered beside the other service gate previews.
5. ~~Add content validation/canonicalization that catches bad bundles before runtime mutation.~~ Done for the owned bootstrap path: authoring-only `drop_tables` accept kill-quest-only rows, and gated service / kill-quest require refs must have an in-bundle writer (`quest_flag` or kill-quest credit). Completely empty tables, orphan gates, and spawn-group + drop-table conflicting kill-quest credit still fail closed (`docs/examples/bootstrap-invalid-conflicting-kill-quest-credit-bundle.json`).
6. Keep deterministic example bundles updated for manual QA; kill-quest-only `drop_tables` authoring now has `docs/examples/bootstrap-kill-quest-only-drop-table-authoring-bundle.json` plus the regen twin `docs/examples/bootstrap-kill-quest-only-regen-authoring-bundle.json` beside the combat+kill-quest fixtures; both kill-quest-only fixtures also have dedicated runtime-import twins (`TestGameRuntimeImportsKillQuestOnlyDropTableAuthoringExample` / `TestGameRuntimeImportsKillQuestOnlyRegenAuthoringExample`); the matching combat+kill-quest fixtures `docs/examples/bootstrap-drop-table-authoring-bundle.json` / `docs/examples/bootstrap-regen-authoring-bundle.json` likewise have dedicated runtime-import twins (`TestGameRuntimeImportsDropTableAuthoringExample` / `TestGameRuntimeImportsRegenAuthoringExample`). `docs/examples/bootstrap-pve-vertical-authoring-bundle.json` now also includes the gated `Warehouse` `open_safebox` actor and formula-first portable combat profile `qa_pve_vertical_practice_mob`, with checked-in expanded twin `docs/examples/bootstrap-pve-vertical-canonical-bundle.json` so one authoring-form import covers warehouse smoke beside merchant / warp / quest turn-in plus authored damage/HP. Kill-quest / PvE / focused `quest_flag` turn-in gameplay proofs now also run on hermetic static/interaction/item/quest MemoryStores rather than disposable content FileStores. Quest-gated and `quest_flag` interaction-visibility preview suites now use the same MemoryStore injection path instead of `QuestStateStorePath` FileStores. Content-bundle runtime export/import/summary suites now use the same MemoryStore injection path as well. Interaction-definition create/upsert/remove runtime suites now also inject hermetic interaction/item MemoryStores (`docs/plans/2026-08-22-hermetic-interaction-definitions-runtime-memorystore-tests.md`). Spawn-homeward content-bundle prune/restore/replace suites now also inject hermetic static/interaction MemoryStores (`docs/plans/2026-08-22-hermetic-spawn-homeward-content-bundle-memorystore-tests.md`). Spawn-return content-bundle prune/restore/replace suites now also inject hermetic static/interaction MemoryStores (`docs/plans/2026-08-22-hermetic-spawn-return-content-bundle-memorystore-tests.md`). Spawn-group operator same-map position MOVE / presentation delete/readd proofs now also inject hermetic static/interaction MemoryStores (`docs/plans/2026-08-24-hermetic-spawn-group-operator-position-move-memorystore-tests.md`). Neighboring shared-world content-bundle import / reject / fanout proofs now also inject hermetic static/interaction/item MemoryStores (`docs/plans/2026-08-24-hermetic-shared-world-content-bundle-import-memorystore-tests.md`). Checked-in negative authoring fixtures cover common reject dry-runs without improvising JSON: `docs/examples/bootstrap-invalid-regen-count-bundle.json` (`count = 2` without `pack_spacing`), `docs/examples/bootstrap-invalid-regen-over-max-count-bundle.json` (`count = 9`), `docs/examples/bootstrap-invalid-regen-one-count-pack-spacing-bundle.json` (`count = 1` with `pack_spacing = 100`), `docs/examples/bootstrap-invalid-colliding-regen-member-refs-bundle.json` (authored `spawn_groups` already owns a synthesized `{ref}.m01` member), and `docs/examples/bootstrap-invalid-orphan-quest-gate-bundle.json` (gated kill-quest-only table missing the `met_guide` writer); `docs/examples/bootstrap-invalid-orphan-service-quest-gate-bundle.json` (gated `shop_preview` missing the `met_guide` writer), and `docs/examples/bootstrap-invalid-empty-drop-table-bundle.json` (completely empty `drop_tables` with no combat channels and no kill-quest credit); `docs/examples/bootstrap-invalid-orphan-require-quest-from-bundle.json` (complete kill-quest credit plus orphan `require_quest_from`); `docs/examples/bootstrap-invalid-partial-require-quest-gate-bundle.json` (`require_quest_ref` without `require_quest_flag`); and `docs/examples/bootstrap-invalid-reverse-partial-require-quest-gate-bundle.json` (`require_quest_flag` without `require_quest_ref`); and `docs/examples/bootstrap-invalid-partial-kill-quest-credit-bundle.json` (incomplete kill-quest credit missing `reward_quest_text`); and `docs/examples/bootstrap-invalid-kill-quest-from-equals-to-bundle.json` (complete kill-quest credit with `reward_quest_from == reward_quest_to`); and `docs/examples/bootstrap-invalid-partial-drop-table-kill-quest-credit-bundle.json` (drop-table incomplete kill-quest credit missing `reward_quest_text`); and `docs/examples/bootstrap-invalid-quest-state-seed-alone-gate-writer-bundle.json` (gated `talk` backed only by portable `quest_state` seed rows, no `quest_flag` / kill-quest writer); and `docs/examples/bootstrap-invalid-partial-service-quest-gate-bundle.json` (service `talk` with partial quest gate `quest_ref` without `quest_flag`); and `docs/examples/bootstrap-invalid-reverse-partial-service-quest-gate-bundle.json` (service `talk` with reverse partial quest gate `quest_flag` without `quest_ref`); and `docs/examples/bootstrap-invalid-orphan-service-quest-from-bundle.json` (ungated service `talk` with orphan `quest_from`); and `docs/examples/bootstrap-invalid-orphan-service-quest-to-bundle.json` (ungated service `talk` with orphan `quest_to`); and `docs/examples/bootstrap-invalid-gated-service-quest-to-bundle.json` (gated service `talk` with complete quest gate plus mutating `quest_to`); and `docs/examples/bootstrap-invalid-dangling-interaction-ref-bundle.json` (static actor `talk` metadata pointing at a missing interaction definition); and `docs/examples/bootstrap-invalid-unsupported-interaction-kind-bundle.json` (static actor `quest` metadata that is not an owned interaction kind); and `docs/examples/bootstrap-invalid-duplicate-static-actor-bundle.json` (two portable static-actor rows that collide after canonical trimming); and `docs/examples/bootstrap-invalid-reward-drop-without-item-templates-bundle.json` (spawn-group item-shaped `reward_drop_vnums` with no bundled `item_templates`); and `docs/examples/bootstrap-invalid-combat-profile-death-reward-without-item-templates-bundle.json` (portable `combat_profiles[].death_reward.drop_vnums` with no bundled `item_templates`); and `docs/examples/bootstrap-invalid-merchant-catalog-without-item-templates-bundle.json` (`shop_preview` catalog with no bundled `item_templates`). Multi-count regen pack placement is now owned authoring: `count` in `2..8` + required `pack_spacing` expands into independent `{ref}.m{NN}` `spawn_groups` on a deterministic grid (`docs/examples/bootstrap-multi-count-regen-authoring-bundle.json`); that focused fixture also has a dedicated runtime-import twin (`TestGameRuntimeImportsMultiCountRegenAuthoringExample`). The composed PvE vertical authoring fixture also authors one denser `practice.qa_pve_vertical_pack` (`count = 2`) beside the gated kill-quest mob. Live runtime still has no pack AI / synchronized respawn / random rectangle placement. The narrow ungated kill-quest credit fixture `docs/examples/bootstrap-kill-quest-credit-bundle.json` and the byte-canonical NPC service fixture `docs/examples/bootstrap-npc-service-bundle.json` now also have dedicated runtime-import twins (`TestGameRuntimeImportsKillQuestCreditExample` / `TestGameRuntimeImportsNpcServiceExample`).

Exit criteria:

- ~~the PvE vertical can be authored with bundle data,~~ done for bootstrap scope via the composed authoring fixture, its checked-in expanded twin `docs/examples/bootstrap-pve-vertical-canonical-bundle.json`, plus the byte-canonical NPC service fixture,
- ~~one quest-style state can persist and resume,~~ done for bootstrap scope (`met_guide` / `killed_qa_mob` survive reconnect and gate services / kill credit),
- ~~spawn/regen/drop definitions are validated before runtime use,~~ done for the owned one-count / fixed-table authoring path,
- unsupported service kinds fail early and clearly.

Anti-goals:

- no full quest-script compatibility in one pass,
- no live reload that can half-commit invalid content,
- no content definitions that bypass store validation.

## Track E — Persistence, DB, migrations, and production ops

**Objective:** move from bootstrap file snapshots toward durable, operable service foundations.

Primary lane:

- `lane/persistence` / `go-metin2-db-ops-worker`

Likely areas:

- `db/migrations`
- `internal/accountstore`
- `internal/loginticket`
- `internal/player`
- `internal/itemstore`
- `internal/config`
- `internal/ops`
- `docs/development.md`
- `docs/workflow.md`
- future deployment/runbook docs

Next slices:

1. Extend the new `db/migrations` catalog beyond the initial `schema_migrations` ledger only when a narrow repository/backfill contract is ready; export quarantine/preflight now exists for `0002`/`0003`/`0004`/`0007`/`0008`/`0009`/`0010`/`0011`/`0012`/`0013`/`0014`/`0015` migration-shaped exports (catalog tip `0020_static_actor_combat_profile_reaction_delay`; safebox money export tip stays `0015_character_safebox_money`; static-actor content tip stays `0013_static_actor_combat_profile_state`), and offline `metin2-migrate quarantine-export` / `backup-restore-drill` / `ledger-snapshot-status` close the runbook gap beside the loopback surfaces. ~~Next: build-tagged SQLite `schema_migrations` driver harness before SQL import/backfill.~~ Done for the build-tagged harness (`go test -tags=sqlite_harness ./db/migrations -run SQLiteHarness`) that applies the catalog on a temp DB and proves live ledger/status/snapshot/plan round-trips — see [SQLite schema_migrations driver-backed harness](2026-08-25-sqlite-schema-migrations-driver-harness.md). ~~Next: first SQL import/backfill for quarantined `0002` roster exports.~~ Done for the programmatic `accountstore.ImportAccountCharacterRoster` seam under `go test -tags=sqlite_harness ./internal/accountstore -run SQLiteHarnessRosterImport` — see [account/character roster SQL import/backfill](2026-08-25-account-character-roster-sql-import-backfill.md). ~~Next: second SQL import/backfill for quarantined `0003` character item-state exports.~~ Done for the programmatic `accountstore.ImportCharacterItemState` seam under `go test -tags=sqlite_harness ./internal/accountstore -run SQLiteHarnessItemStateImport` — see [character item-state SQL import/backfill](2026-08-26-character-item-state-sql-import-backfill.md). ~~Next: third SQL import/backfill for quarantined `0004` character quest-state exports.~~ Done for the programmatic `queststate.ImportCharacterQuestState` seam under `go test -tags=sqlite_harness ./internal/queststate -run SQLiteHarnessQuestStateImport` — see [character quest-state SQL import/backfill](2026-08-26-character-quest-state-sql-import-backfill.md). ~~Next: fourth SQL import/backfill for quarantined `0011` character point-state exports.~~ Done for the programmatic `accountstore.ImportCharacterPointState` seam under `go test -tags=sqlite_harness ./internal/accountstore -run SQLiteHarnessPointStateImport` — see [character point-state SQL import/backfill](2026-08-26-character-point-state-sql-import-backfill.md). ~~Next: fifth SQL import/backfill for quarantined `0015` character safebox-state exports.~~ Done for the programmatic `safeboxstore.ImportCharacterSafeboxState` seam under `go test -tags=sqlite_harness ./internal/safeboxstore -run SQLiteHarnessSafeboxStateImport` — see [character safebox-state SQL import/backfill](2026-08-26-character-safebox-state-sql-import-backfill.md). ~~Next: sixth SQL import/backfill for quarantined `0010` bootstrap ground-item-state exports.~~ Done for the programmatic `worldruntime.ImportBootstrapGroundItemState` seam under `go test -tags=sqlite_harness ./internal/worldruntime -run SQLiteHarnessGroundItemStateImport` — see [bootstrap ground-item-state SQL import/backfill](2026-08-26-bootstrap-ground-item-state-sql-import-backfill.md). ~~Next: seventh SQL import/backfill for quarantined `0009` item-template-state exports.~~ Done for the programmatic `itemstore.ImportItemTemplateState` seam under `go test -tags=sqlite_harness ./internal/itemstore -run SQLiteHarnessItemTemplateStateImport` — see [item-template-state SQL import/backfill](2026-08-26-item-template-state-sql-import-backfill.md). ~~Next: eighth SQL import/backfill for quarantined `0007` auth-login-ticket-handoff exports.~~ Done for the programmatic `loginticket.ImportAuthLoginTicketHandoff` seam under `go test -tags=sqlite_harness ./internal/loginticket -run SQLiteHarnessAuthLoginTicketHandoffImport` — see [auth-login-ticket-handoff SQL import/backfill](2026-08-26-auth-login-ticket-handoff-sql-import-backfill.md). ~~Next: ninth SQL import/backfill for quarantined tip-`0013` static-actor content-state exports.~~ Done for the programmatic `staticstore.ImportStaticActorContentState` seam under `go test -tags=sqlite_harness ./internal/staticstore -run SQLiteHarnessStaticActorContentStateImport` — see [static-actor content-state SQL import/backfill](2026-08-27-static-actor-content-state-sql-import-backfill.md). ~~Next: confirmation-gated `metin2-migrate import-export` CLI wiring.~~ Done for `metin2-migrate import-export` under `go test ./internal/migratecli -run ImportExport` — see [CLI import-export](2026-08-27-cli-import-export.md). ~~Next: confirmation-gated print-only `import-export-drill`.~~ Done for `metin2-migrate import-export-drill` under `go test ./internal/migratecli -run ImportExportDrill` — see [CLI import-export drill printer](2026-08-27-cli-import-export-drill.md). ~~Next: hermetic `/bin/sh` import-export-drill against SQLite.~~ Done under `go test -tags=sqlite_harness ./internal/migratecli -run ImportExportDrillSQLite` — see [hermetic import-export drill SQLite execution proof](2026-08-27-hermetic-import-export-drill-sqlite-execution-proof.md). ~~Next: seeded non-empty hermetic import-export-drill against SQLite.~~ Done under the same `-run ImportExportDrillSQLite` filter — see [hermetic import-export drill SQLite seeded tree](2026-08-27-hermetic-import-export-drill-sqlite-seeded-tree.md). ~~Next: hermetic `/bin/sh` migration-run-retention printed-script apply/rollback against SQLite plus soft-fail leftover-lock triage.~~ Done under `go test -tags=sqlite_harness ./internal/migratecli -run MigrationRunRetentionSQLite` — see [hermetic migration-run-retention SQLite apply](2026-08-28-hermetic-migration-run-retention-sqlite-apply.md); missing `schema_migrations` now reads as an empty ledger for first-time snapshot/status. ~~Next: hermetic intermediate-target retention (empty→`7`, tip→`8`).~~ Done under the same `-run MigrationRunRetentionSQLite` filter — see [hermetic migration-run-retention intermediate-target SQLite](2026-08-28-hermetic-migration-run-retention-intermediate-target-sqlite.md). Stock binaries still do not register a production driver; ~~contrib print-helper target/rollback env forwarding~~ Done — see [contrib migration-run retention target env](2026-08-28-contrib-migration-run-retention-target-env.md); ~~Next: read-only `import-export-status` for retained `import-result.json`.~~ Done — see [CLI import-export status](2026-08-28-cli-import-export-status.md). ~~Next: hermetic `/bin/sh` proof that printed `import-export-status` redirects write valid `import-result-status.json`.~~ Done under `go test -tags=sqlite_harness ./internal/migratecli -run ImportExportDrillSQLite` — see [hermetic import-export status SQLite proof](2026-08-28-hermetic-import-export-status-sqlite.md). ~~Next: fail-closed static-actor import schema gate for additive `0016` chase-delay column.~~ Done under `go test -tags=sqlite_harness ./internal/staticstore -run SQLiteHarnessStaticActorContentStateImport` — see [static-actor import require chase-delay schema](2026-08-28-static-actor-import-require-chase-delay-schema.md). ~~Next: fail-closed static-actor import schema gate for additive `0017` return-delay column + catalog-tip docs sync.~~ Done under the same `-run SQLiteHarnessStaticActorContentStateImport` filter (tip-`0016`-only reject) — see [static-actor import require return-delay schema](2026-08-28-static-actor-import-require-return-delay-schema.md). ~~Next: fail-closed static-actor import schema gate for additive `0018` homeward-delay column + catalog-tip docs sync.~~ Done under the same `-run SQLiteHarnessStaticActorContentStateImport` filter (tip-`0017`-only reject) — see [static-actor import require homeward-delay schema](2026-08-28-static-actor-import-require-homeward-delay-schema.md). ~~Next: fail-closed static-actor import schema gate for additive `0019` max-step column + catalog-tip docs sync.~~ Done under the same `-run SQLiteHarnessStaticActorContentStateImport` filter (tip-`0018`-only reject) — see [static-actor import require max-step schema](2026-08-29-static-actor-import-require-max-step-schema.md); upsert/merge policy and production-engine selection remain deferred.
2. ~~Extract narrow repository seams only where tests prove reduced coupling.~~ Done for the first account character-state seam (`0002` roster / `0003` item-state / `0011` point-state), the matching quest-state seam (`0004`), the auth login-ticket handoff seam (`0007`), the item-template seam (`0009`), the static-content seam (`0008`), the bootstrap ground-handle seam (`0010`), and the durable safebox seam (`0014` / tip `0015`): named `AccountCharacterStateExporter` / `CharacterQuestStateExporter` / `AuthLoginTicketHandoffExporter` / `ItemTemplateStateExporter` / `StaticActorContentStateExporter` / `BootstrapGroundItemStateExporter` / `CharacterSafeboxStateExporter` plus hermetic `MemoryStore`s (including paired `interactionstore.MemoryStore`, `worldruntime.MemoryGroundItemStore` / `SnapshotGroundItemExporter`, and `safeboxstore.MemoryStore`); SQL-backed repositories remain follow-on.
3. ~~Write backup/restore runbooks backed by local validation or preflight tests.~~ Done for the eight manifested stores (six bootstrap JSON stores plus the durable pending ground-item and safebox FileStores) plus the read-only drill printer: status/runtime-config reporting, BackupTo/RestoreFrom, crash-temp triage, and `metin2-migrate backup-restore-drill` curls are owned through `/local/ground-item-store/*` and `/local/safebox-store/*` beside the older store routes.
4. ~~Harden crash/restart recovery for item/gold/character state used by the PvE loop.~~ Done for committed FileStore rematerialization of character gold, inventory, and quest flags after a `quest_flag` reward turn-in (`TestGameRuntimeQuestFlagRewardStateRematerializesAcrossDaemonRestart`), equipment and quickslots after live equip/bind (`TestGameRuntimeEquipmentAndQuickslotsRematerializeAcrossDaemonRestart`), map/x/y and character point-state after live item-use + transfer (`TestGameRuntimePositionAndPointsRematerializeAcrossDaemonRestart`), pending bootstrap ground item/gold handles with absolute ownership/despawn timers (`TestGameRuntimePendingGroundItemAndGoldRematerializeAcrossDaemonRestart`), durable same-account safebox cells across restart (`TestGameRuntimeSafeboxCheckinSurvivesProcessRestartRematerializeOnOpen`), and durable warehouse money across restart (`TestGameRuntimeSafeboxMoneySurvivesProcessRestartRematerializeOnOpen`); SQL import/backfill from quarantined `0014`/`0015` safebox exports and quarantined `0010` ground-item-state exports and quarantined `0009` item-template-state exports and quarantined `0007` auth-login-ticket-handoff exports is now owned beside the `0002`/`0003`/`0004`/`0011` seams; tip-`0013` static-actor content-state SQL import is now owned; confirmation-gated `metin2-migrate import-export` CLI wiring is now owned for the tip kinds (see [CLI import-export](2026-08-27-cli-import-export.md)); confirmation-gated print-only `metin2-migrate import-export-drill` is now owned for retained export/quarantine trees (see [CLI import-export drill printer](2026-08-27-cli-import-export-drill.md)); env-gated print-only `import-export-drill.sh` via `contrib/lab-retention-gc/` is now owned (see [contrib import-export drill print helper](2026-08-27-contrib-import-export-drill-print-helper.md)); hermetic `/bin/sh` execution of that printed import drill against build-tagged SQLite is now owned (see [hermetic import-export drill SQLite execution proof](2026-08-27-hermetic-import-export-drill-sqlite-execution-proof.md)); seeded non-empty hermetic import-export-drill coverage is now owned beside that empty-tree proof (see [hermetic import-export drill SQLite seeded tree](2026-08-27-hermetic-import-export-drill-sqlite-seeded-tree.md)); hermetic intermediate-target `migration-run-retention` (empty→`7`, tip→`8`) is now owned beside tip/zero (see [hermetic migration-run-retention intermediate-target SQLite](2026-08-28-hermetic-migration-run-retention-intermediate-target-sqlite.md)); ~~contrib print-helper target/rollback env forwarding~~ Done — see [contrib migration-run retention target env](2026-08-28-contrib-migration-run-retention-target-env.md); ~~hermetic import-export-status redirect proof~~ Done — see [hermetic import-export status SQLite proof](2026-08-28-hermetic-import-export-status-sqlite.md). ~~Next: fail-closed static-actor import schema gate for additive `0016` chase-delay column.~~ Done under `go test -tags=sqlite_harness ./internal/staticstore -run SQLiteHarnessStaticActorContentStateImport` — see [static-actor import require chase-delay schema](2026-08-28-static-actor-import-require-chase-delay-schema.md). ~~Next: fail-closed static-actor import schema gate for additive `0017` return-delay column + catalog-tip docs sync.~~ Done under the same `-run SQLiteHarnessStaticActorContentStateImport` filter (tip-`0016`-only reject) — see [static-actor import require return-delay schema](2026-08-28-static-actor-import-require-return-delay-schema.md). ~~Next: fail-closed static-actor import schema gate for additive `0018` homeward-delay column + catalog-tip docs sync.~~ Done under the same `-run SQLiteHarnessStaticActorContentStateImport` filter (tip-`0017`-only reject) — see [static-actor import require homeward-delay schema](2026-08-28-static-actor-import-require-homeward-delay-schema.md). ~~Next: fail-closed static-actor import schema gate for additive `0019` max-step column + catalog-tip docs sync.~~ Done under the same `-run SQLiteHarnessStaticActorContentStateImport` filter (tip-`0018`-only reject) — see [static-actor import require max-step schema](2026-08-29-static-actor-import-require-max-step-schema.md); upsert/merge policy and production-engine selection remain deferred.
5. ~~Document release/deploy/versioning policy;~~ Done for release identity (`internal/buildinfo`, loopback `/local/build-info`, `metin2-migrate version`) plus the first lab host layout and artifact-retention trees in `docs/workflow/lab-deployment-topology.md`; multi-host / orchestrated deploy automation remains follow-on work. Lab leftover-lock recovery is now owned beside that topology in `docs/workflow/lab-stale-lock-recovery.md` with an advisory `manual_clear_candidate` bit on `apply-lock-status` (still no auto-delete). Print-only systemd / FreeBSD cron / FreeBSD `periodic(8)` `.sample` units that only dump retention/GC printer stdout under `/var/metin2/ops-prints/` are owned in `docs/workflow/lab-retention-gc-unit-samples.md`, with disabled-by-default tree fragments under `contrib/lab-retention-gc/` (including `periodic/weekly/900.metin2-artifact-retention-gc-print.sample` gated on `weekly_metin2_artifact_retention_gc_print_enable="NO"`; still no auto-execution of printed scripts). Disabled-by-default FreeBSD `rc.d` + systemd samples for `authd` / `gamed` are owned under `contrib/lab-daemons/` gated on `authd_enable="NO"` / `gamed_enable="NO"` (see `docs/workflow/lab-daemon-unit-samples.md`), including optional `/var/log/metin2/{authd,gamed}.log` capture plus reviewable `newsyslog` / `logrotate` fragments; FreeBSD port / `pkg` enable defaults remain deferred. Offline `backup-restore-drill` / `migration-run-retention` printers optionally retain those daemon JSON log files into each lab retention tree (`--gamed-log-path` / `--authd-log-path`; missing files stay non-fatal) — see [CLI daemon log retention correlation](2026-08-24-cli-daemon-log-retention-correlation.md). The tree-owned print-only helper under `contrib/lab-retention-gc/` forwards the same flags from `METIN2_GAMED_LOG_PATH` / `METIN2_AUTHD_LOG_PATH` (defaults `/var/log/metin2/{gamed,authd}.log`) — see [contrib retention helper daemon log paths](2026-08-24-contrib-retention-helper-daemon-log-paths.md). Env-gated print-only `import-export-drill.sh` via `METIN2_IMPORT_EXPORT_TREE` / `METIN2_IMPORT_DRIVER` (optional `METIN2_IMPORT_DSN_ENV` name only) is now owned by that helper — see [contrib import-export drill print helper](2026-08-27-contrib-import-export-drill-print-helper.md). Confirmation-gated print-only `metin2-migrate artifact-gc-aside-purge` closes the deferred `rm` of aged `.gc-aside-*` trees without auto-execution — see [CLI artifact GC-aside purge printer](2026-08-25-cli-artifact-gc-aside-purge-printer.md). ~~Folding purge into `contrib/lab-retention-gc`~~ Done — see [contrib artifact GC-aside purge print helper](2026-08-27-contrib-artifact-gc-aside-purge-print-helper.md). Automatic / scheduled purge execution remains deferred.
6. ~~Add production-safe observability conventions before remote admin surfaces.~~ Done for daemon JSON logging via `internal/observability.NewServiceLogger` and `docs/workflow/production-observability.md` (identity attrs + sensitive-key redaction) plus metadata-only loopback `/local/*` access logs via `observability.WrapOpsAccessLog` (no bodies/query strings; `/healthz` and `/debug/pprof/*` stay quiet); metrics/tracing/remote log shipping remain follow-on work.

Exit criteria:

- migrations are repeatable,
- operators can validate and back up/restore key state,
- core gameplay state has a clear path from file-backed bootstrap to DB-backed storage,
- sensitive ops remain loopback/local-only unless an explicit auth design exists.

Anti-goals:

- no big-bang DB migration,
- no secret-bearing production config in git,
- no remote admin API without authentication and threat model.

## Track F — Social systems after the PvE loop

**Objective:** replace bootstrap chat fanout with real social state after the PvE loop has enough gameplay to share.

Likely future areas:

- `internal/proto/chat`
- future party/guild/messenger packages
- `internal/minimal`
- `internal/worldruntime`
- `spec/protocol/party-*`
- `spec/protocol/guild-*`

Next slices after PvE stabilizes:

1. Party membership state.
2. Invite/accept/leave/kick where packet evidence exists.
3. Party chat scoping based on membership rather than bootstrap fanout.
4. Party EXP/drop sharing after reward ownership is stable.
5. Guild roster/rank state.
6. Friend/messenger/block systems.

Exit criteria:

- party/guild chat is membership-scoped,
- gameplay systems can query social state safely,
- membership persistence is either implemented or explicitly scoped.

Anti-goals:

- no social effects before membership exists,
- no claims of real party/guild support while fanout remains bootstrap-shaped.

## Integration and validation model

Autonomous lanes should continue to produce small commits on lane branches. The integrator should be the only job that pushes `origin/main`.

Recommended lane mapping:

- `lane/items` — Track C
- `lane/combat` — Track B
- `lane/world` — Track A
- `lane/content` — Track D
- `lane/persistence` — Track E
- `main` — integration only

Validation expectations:

- focused tests for the touched slice,
- `gofmt` clean,
- `git diff --check`,
- `go test ./...` when the integration environment can complete it reliably,
- `go vet ./...`,
- daemon builds before pushing code changes to `main`.

If the full suite times out in filesystem-heavy `internal/minimal` file-store tests, treat that as an infrastructure/timeout problem to fix deliberately. Do not push code changes that have not passed the agreed integration gate; for documentation-only changes, at minimum validate links, formatting, and repository reality.
